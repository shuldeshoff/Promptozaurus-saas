# PROMPTYFLOW SAAS - Техническое задание и описание сервиса
**Сайт: http://promptyflow.com/**

---

## 1. ОБЩАЯ КОНЦЕПЦИЯ

**PromptyFlow SaaS** — веб-приложение для создания, организации и управления промптами для AI-моделей. Трансформация desktop Electron-приложения в многопользовательский облачный сервис с сохранением всей ключевой функциональности.

### 1.1 Модель распространения
- **Бесплатный план** с ограничениями (до 10 проектов на пользователя)
- **Архитектура готова к масштабированию** для будущих платных планов

### 1.2 Языковая поддержка
- Английский (по умолчанию)
- Русский
- Архитектура позволяет добавление дополнительных языков

### 1.3 Визуальная тема
- Темная тема (по умолчанию)
- Архитектура позволяет добавление светлой темы

---

## 2. АРХИТЕКТУРА И СТРУКТУРА ДАННЫХ

### 2.1 Иерархия объектов

```
ПОЛЬЗОВАТЕЛЬ (User Account)
│
├── Профиль
│   ├── Email (Google OAuth)
│   ├── Имя
│   ├── Язык интерфейса (en/ru)
│   ├── Тема (dark/light)
│   └── Дата регистрации
│
├── API Ключи (зашифрованное хранилище)
│   ├── OpenAI
│   ├── Anthropic (Claude)
│   ├── Google Gemini
│   ├── X.AI (Grok)
│   └── OpenRouter
│
├── Библиотека шаблонов промптов (Personal Template Library)
│   └── Шаблоны [*.txt]
│       ├── Название
│       ├── Содержимое (текст шаблона)
│       └── Дата создания/изменения
│
└── Проекты (Projects) [максимум 10 в бесплатной версии]
    └── Проект
        ├── Название проекта
        ├── Дата создания/изменения
        │
        ├── Контекстные блоки (Context Blocks)
        │   └── Блок контекста
        │       ├── Название блока
        │       │
        │       └── Элементы (Items)
        │           └── Элемент
        │               ├── Название элемента
        │               ├── Содержимое
        │               ├── Счетчик символов
        │               │
        │               └── Подэлементы (SubItems)
        │                   └── Подэлемент
        │                       ├── Название
        │                       ├── Содержимое
        │                       └── Счетчик символов
        │
        └── Промпт-блоки (Prompt Blocks)
            └── Промпт
                ├── Название промпта
                ├── Шаблон промпта (с плейсхолдером {{context}})
                ├── Связь с шаблоном из библиотеки (опционально)
                └── Выбранные контексты
                    └── Ссылки на блоки/элементы/подэлементы
```

### 2.2 Ключевые отличия от desktop версии

**Что меняется:**
- Файловая система → База данных (хранение всех данных на сервере)
- Локальное хранилище API-ключей (keytar) → Зашифрованное серверное хранилище
- Один пользователь → Многопользовательская система с аутентификацией
- Неограниченные проекты → Лимит 10 проектов (бесплатный план)
- Нет управления папками → Все данные в облаке

**Что сохраняется:**
- Трехуровневая структура контекста (Блок → Элемент → Подэлемент)
- Механизм компиляции промптов с плейсхолдером {{context}}
- Выбор контекстов через чекбоксы
- Библиотека шаблонов промптов
- Интеграция с 5 AI-провайдерами
- Счетчики символов
- Функция разделения текста на части
- Копирование результата в буфер обмена

---

## 3. АВТОРИЗАЦИЯ И АККАУНТ ПОЛЬЗОВАТЕЛЯ

### 3.1 Процесс входа (Google OAuth 2.0)

**Шаг 1: Первое посещение**
1. Пользователь открывает сайт
2. Видит экран приветствия с кнопкой "Войти через Google"
3. Нажимает кнопку

**Шаг 2: Google OAuth**
1. Редирект на страницу авторизации Google
2. Пользователь выбирает аккаунт Google или входит
3. Дает разрешение приложению (email, имя, фото профиля)
4. Редирект обратно в приложение

**Шаг 3: Создание/загрузка аккаунта**
- **Новый пользователь:**
  - Система создает аккаунт в БД
  - Email, имя, фото профиля из Google
  - Создается пустая личная библиотека шаблонов
  - Язык определяется автоматически (браузер) или устанавливается английский
  - Пользователь попадает в пустой интерфейс с приглашением создать первый проект

- **Существующий пользователь:**
  - Система загружает данные из БД
  - Восстанавливается последнее состояние (открытый проект, активные блоки)
  - Пользователь видит свои проекты и данные

### 3.2 Профиль пользователя

**Доступ:** Клик на аватар в правом верхнем углу

**Настройки профиля:**
- **Личные данные:**
  - Имя (из Google, возможность редактирования)
  - Email (из Google, только для отображения)
  - Фото профиля (из Google)
  - Дата регистрации

- **Настройки интерфейса:**
  - Выбор языка (EN/RU) — выпадающий список
  - Выбор темы (Dark/Light) — переключатель

- **API Ключи:**
  - Форма для добавления/редактирования ключей 5 провайдеров
  - Кнопка "Тест подключения" для каждого провайдера
  - Статус: Не настроено / Активно / Ошибка
  - Ключи хранятся в зашифрованном виде на сервере

- **Управление аккаунтом:**
  - Количество проектов: X / 10 (бесплатный план)
  - Кнопка "Выйти"

### 3.3 Ограничения бесплатного плана

- **Проекты:** максимум 10
- **Контекстные блоки в проекте:** без ограничений
- **Промпты в проекте:** без ограничений
- **Шаблоны в личной библиотеке:** без ограничений
- **Размер данных:** без ограничений (пока бесплатно)

При попытке создать 11-й проект:
- Появляется уведомление: "Достигнут лимит проектов для бесплатного плана (10). Удалите один из существующих проектов или обновите план."
- Кнопка создания проекта становится неактивной

---

## 4. ОСНОВНОЙ ИНТЕРФЕЙС

### 4.1 Общая структура (аналогично desktop версии)

┌─────────────────────────────────────────────────────────────────┐
│  HEADER                                                          │
│  [Логотип] [Проекты▼] [Библиотека] [AI] │ [Язык▼] [Аватар▼]   │
├──────────────┬──────────────────────┬──────────────────────────┤
│ Левая панель │  Центральная панель  │   Правая панель          │
│ (Навигация)  │  (Блоки)             │   (Редактор)             │
│              │                      │                          │
│ - Проекты    │ Вкладки:             │ Активный элемент:        │
│   [Список]   │ • Контекст           │ • Редактор контекста     │
│              │ • Промпт             │ • Редактор промпта       │
│ + Новый      │                      │                          │
│   проект     │ [Список блоков]      │ [Поле редактирования]    │
│              │ + Добавить блок      │ [Кнопки действий]        │
│              │                      │                          │
│              │                      │                          │
└──────────────┴──────────────────────┴──────────────────────────┘


ВОЗМОЖНО в центральной панели нужно отражать ТОЛЬКО текущий активный блок контекста или промпта, а не все сразу как сейчас (обсуждается)

### 4.2 Адаптация размеров панелей

- Разделители между панелями для изменения ширины
- Пропорции сохраняются в localStorage браузера
- Минимальная ширина каждой панели для предотвращения коллапса
- На мобильных устройствах — вертикальное расположение с табами

### 4.3 Header (Верхняя панель)

**Левая часть:**
- **Логотип** "PromptyFlow" — клик возвращает на главный экран (Название проекта обсуждается)
- **Выпадающее меню "Проекты":**
  - Список всех проектов пользователя
  - "+ Новый проект"
  - Показывает: X / 10 проектов
- **Кнопка "Библиотека"** — открывает модальное окно личной библиотеки шаблонов
- **Кнопка "AI"** — быстрый доступ к настройкам AI провайдеров

**Правая часть:**
- **Выпадающий список языков:** EN | RU
- **Аватар пользователя (из Google):**
  - Клик открывает меню профиля
  - Опции: Настройки | API Ключи | Выйти

### 4.4 Левая панель (Навигация)

**Список проектов:**
- Отображаются все созданные проекты (до 10)
- Каждый проект показывает:
  - Название
  - Дата последнего изменения
  - Количество контекстных блоков / промптов (опционально)
- Активный проект подсвечен
- Клик на проект — загружает его в рабочую область

**Действия:**
- **"+ Новый проект"** — создает новый проект (если лимит не превышен)
- **Контекстное меню на проекте** (правый клик или три точки):
  - Переименовать
  - Дублировать
  - Экспорт в JSON
  - Удалить

**Кнопка внизу:**
- **"Импорт проекта"** — загрузка проекта из JSON файла

### 4.5 Центральная панель (Блоки)

**Вкладки:**
- **"Контекст"** — управление контекстными блоками
- **"Промпт"** — управление промпт-блоками

#### Вкладка "Контекст"

**Список контекстных блоков:**
- Каждый блок — карточка с:
  - Название блока (редактируемое по двойному клику)
  - Количество элементов
  - Общее количество символов
  - Кнопка "🗑️" (удалить блок)

**Раскрывающийся список элементов внутри блока:**
- Чекбокс для выбора элемента (для включения в промпт)
- Название элемента
- Количество символов
- Иконка ">" для раскрытия подэлементов
- Кнопка "✏️" — редактировать (загружает в правую панель)
- Кнопка "🗑️" — удалить элемент

**Подэлементы (если есть):**
- Чекбокс
- Название подэлемента
- Количество символов
- Кнопка "✏️" — редактировать
- Кнопка "🗑️" — удалить

**Кнопки действий:**
- **"+ Добавить блок контекста"** — создает новый блок
- Внутри блока:
  - **"+ Добавить элемент"** — создает элемент
  - Внутри элемента:
    - **"+ Добавить подэлемент"** — создает подэлемент

#### Вкладка "Промпт"

**Список промпт-блоков:**
- Каждый промпт — карточка с:
  - Название промпта (редактируемое)
  - Количество символов в шаблоне
  - Количество символов выбранного контекста
  - Кнопка "📋" — копировать скомпилированный промпт
  - Кнопка "🗑️" — удалить промпт

**Клик на промпт:**
- Открывает его в правой панели для редактирования

**Кнопки действий:**
- **"+ Добавить промпт"** — создает новый промпт-блок

### 4.6 Правая панель (Редактор)

#### Режим редактирования контекста

**Когда выбран элемент или подэлемент:**

**Верхняя часть:**
- Breadcrumb навигация: `Блок > Элемент > Подэлемент`
- Поле "Название" (текстовый input)

**Текстовое поле:**
- Большое textarea для содержимого
- Проверка орфографии (если браузер поддерживает)
- Счетчик символов снизу: "Символов: XXXX"

**Кнопки действий:**
- **"Сохранить"** — сохраняет изменения (или автосохранение)
- **"Разделить текст"** — открывает модальное окно разделения
- **"Отмена"** — сбрасывает изменения (если есть несохраненные)

**Модальное окно "Разделить текст":**
- Опции разделения:
  - По количеству символов (input: число символов)
  - По параграфам (разделение по \n\n)
  - По предложениям (разделение по . ! ?)
  - По кастомному шаблону (regex поле)
- Превью: показывает количество частей, которые получатся
- Кнопка "Применить" — создает подэлементы из частей
- Кнопка "Отмена"

#### Режим редактирования промпта

**Когда выбран промпт-блок:**

**Верхняя часть:**
- Поле "Название промпта" (текстовый input)

**Секция "Шаблон промпта":**
- Textarea для шаблона
- Подсказка: "Используйте {{context}} для вставки контекста"
- Кнопки:
  - **"📁 Загрузить из библиотеки"** — открывает модальное окно выбора шаблона
  - **"💾 Сохранить в библиотеку"** — сохраняет текущий шаблон в личную библиотеку

**Секция "Выбор контекста":**
- Список всех контекстных блоков проекта с чекбоксами:
  - Уровень 1: Блок (чекбокс — выбрать весь блок)
    - Уровень 2: Элемент (чекбокс)
      - Уровень 3: Подэлемент (чекбокс)
- Счетчик: "Выбрано символов: XXXX"

**Секция "Результат":**
- Readonly textarea с скомпилированным промптом
- Кнопки:
  - **"Скомпилировать"** — обновляет результат (обычный режим)
  - **"Скомпилировать с тегами"** — обновляет результат с XML обертками
  - **"📋 Копировать результат"** — копирует в буфер обмена

**Секция "Отправка в AI" (если настроен хотя бы один провайдер):**
- Выпадающий список моделей (из всех настроенных провайдеров)
- Кнопка **"Отправить"** — отправляет скомпилированный промпт через API
- Поле для отображения ответа (если была отправка)

---

## 5. БИБЛИОТЕКА ШАБЛОНОВ ПРОМПТОВ

### 5.1 Назначение
Личное хранилище пользователя для многократно используемых шаблонов промптов.

### 5.2 Доступ
- Кнопка "Библиотека" в Header
- Кнопка "📁 Загрузить из библиотеки" в редакторе промпта

### 5.3 Модальное окно библиотеки

**Структура:**
- Список шаблонов (карточки):
  - Название шаблона
  - Первые 100 символов содержимого (превью)
  - Дата создания
  - Кнопка "Использовать" — загружает в текущий промпт
  - Кнопка "✏️" — редактировать
  - Кнопка "🗑️" — удалить

**Действия:**
- **"+ Создать шаблон"** — открывает форму создания
- **Поле поиска** — фильтрует шаблоны по названию

**Форма создания/редактирования шаблона:**
- Поле "Название"
- Textarea "Содержимое"
- Кнопка "Сохранить"
- Кнопка "Отмена"

---

## 6. РАБОТА С ПРОЕКТАМИ

### 6.1 Создание нового проекта

**Действие:** Клик на "+ Новый проект"

**Процесс:**
1. Проверка лимита (10 проектов)
2. Если лимит превышен → уведомление и блокировка
3. Если лимит не превышен:
   - Открывается модальное окно "Новый проект"
   - Поле "Название проекта" (обязательное)
   - Кнопка "Создать"
4. Создается проект в БД с пустыми массивами contextBlocks и promptBlocks
5. Проект открывается в интерфейсе

### 6.2 Открытие проекта

**Действие:** Клик на проект в левой панели или в выпадающем меню "Проекты"

**Процесс:**
1. Загрузка данных проекта из БД
2. Восстановление состояния:
   - Последний активный контекстный блок
   - Последний активный промпт
   - Выбранные чекбоксы
3. Отображение в интерфейсе

### 6.3 Сохранение проекта

**Режим автосохранения:**
- Все изменения сохраняются в БД автоматически через debounce (например, 2 секунды после последнего ввода)
- Индикатор состояния: "Сохранено" / "Сохранение..." в Header

**Ручное сохранение:**
- Горячая клавиша Ctrl+S (опционально, дублирует автосохранение)

### 6.4 Переименование проекта

**Действие:** Контекстное меню → "Переименовать"

**Процесс:**
1. Открывается prompt с текущим названием
2. Пользователь вводит новое название
3. Подтверждение → обновление в БД и интерфейсе

### 6.5 Дублирование проекта

**Действие:** Контекстное меню → "Дублировать"

**Процесс:**
1. Проверка лимита (10 проектов)
2. Если лимит превышен → уведомление
3. Если лимит не превышен:
   - Создается копия всей структуры проекта
   - Название: "Копия [Исходное название]"
   - Копия добавляется в список проектов

### 6.6 Экспорт проекта

**Действие:** Контекстное меню → "Экспорт в JSON"

**Процесс:**
1. Сериализация всех данных проекта в JSON
2. Браузер инициирует скачивание файла: `[название_проекта].json`
3. Формат идентичен desktop версии для совместимости

### 6.7 Импорт проекта

**Действие:** Кнопка "Импорт проекта" внизу левой панели

**Процесс:**
1. Проверка лимита (10 проектов)
2. Если лимит превышен → уведомление
3. Если лимит не превышен:
   - Открывается диалог выбора файла (только .json)
   - Пользователь выбирает файл
   - Валидация структуры JSON
   - Если валидация успешна:
     - Создается новый проект с импортированными данными
     - Миграция данных (добавление subItems/subItemIds если отсутствуют)
     - Проект добавляется в список
   - Если ошибка → уведомление с описанием проблемы

### 6.8 Удаление проекта

**Действие:** Контекстное меню → "Удалить"

**Процесс:**
1. Модальное окно подтверждения: "Вы уверены, что хотите удалить проект '[Название]'? Это действие необратимо."
2. Кнопки: "Удалить" (красная) | "Отмена"
3. При подтверждении:
   - Удаление из БД
   - Удаление из интерфейса
   - Если был активен — открывается первый доступный проект или пустое состояние

---

## 7. РАБОТА С КОНТЕКСТНЫМИ БЛОКАМИ

### 7.1 Создание блока контекста

**Действие:** "+ Добавить блок контекста"

**Процесс:**
1. Создается новый блок с названием "Новый контекст N"
2. Блок добавляется в список
3. Фокус переходит на название для редактирования

### 7.2 Редактирование названия блока

**Действие:** Двойной клик на название блока

**Процесс:**
1. Название становится редактируемым (inline editing)
2. Пользователь вводит новое название
3. Enter или потеря фокуса → сохранение
4. Escape → отмена изменений

### 7.3 Создание элемента контекста

**Действие:** Внутри блока → "+ Добавить элемент"

**Процесс:**
1. Создается новый элемент с названием "Новый элемент N"
2. Элемент добавляется в список элементов блока
3. Автоматически открывается в правой панели для редактирования

### 7.4 Создание подэлемента

**Действие:** Внутри элемента → "+ Добавить подэлемент"

**Процесс:**
1. Создается новый подэлемент с названием "Новый подэлемент N"
2. Подэлемент добавляется в список подэлементов элемента
3. Автоматически открывается в правой панели для редактирования

### 7.5 Редактирование содержимого

**Действие:** Клик на "✏️" или клик на название элемента/подэлемента

**Процесс:**
1. Правая панель переключается в режим редактирования
2. Отображается:
   - Breadcrumb навигация
   - Поле "Название"
   - Textarea для содержимого
   - Счетчик символов (обновляется в реальном времени)
3. Автосохранение при изменениях
4. Обновление счетчика символов в списке блоков

### 7.6 Разделение текста на подэлементы

**Действие:** Кнопка "Разделить текст" в редакторе элемента

**Процесс:**
1. Открывается модальное окно с опциями:
   - **По символам:** input для числа символов на часть
   - **По параграфам:** разделение по двойному переносу строки
   - **По предложениям:** разделение по точке + пробел
   - **По шаблону:** поле для ввода разделителя (строка или regex)
2. Кнопка "Превью" → показывает сколько частей получится
3. Кнопка "Применить":
   - Текст разделяется на части
   - Для каждой части создается подэлемент
   - Названия подэлементов: "Часть 1", "Часть 2" и т.д.
   - Исходное содержимое элемента очищается (опционально)
4. Модальное окно закрывается

### 7.7 Удаление блока/элемента/подэлемента

**Действие:** Клик на "🗑️"

**Процесс:**
1. Модальное окно подтверждения: "Удалить [тип]: '[Название]'?"
2. При подтверждении:
   - Удаление из БД
   - Удаление из списка в интерфейсе
   - Если был открыт в редакторе → очистка правой панели

### 7.8 Подсчет символов

**Автоматический подсчет:**
- При вводе текста в textarea
- Обновление в реальном времени
- Отображение:
  - В карточке блока: сумма всех элементов и подэлементов
  - В карточке элемента: содержимое элемента + сумма подэлементов
  - В карточке подэлемента: содержимое подэлемента
  - В редакторе: "Символов: XXXX"

---

## 8. РАБОТА С ПРОМПТАМИ

### 8.1 Создание промпт-блока

**Действие:** "+ Добавить промпт"

**Процесс:**
1. Создается новый промпт с названием "Новый промпт N"
2. Пустой шаблон
3. Нет выбранных контекстов
4. Промпт открывается в правой панели

### 8.2 Редактирование шаблона промпта

**Действие:** Клик на промпт в списке

**Процесс:**
1. Правая панель переключается в режим редактирования промпта
2. Поле "Название промпта" — редактируемое
3. Textarea "Шаблон промпта":
   - Пользователь вводит текст шаблона
   - Использует плейсхолдер `{{context}}` в нужном месте
   - Подсказка под полем: "Используйте {{context}} для вставки контекста"
4. Автосохранение при изменениях

### 8.3 Загрузка шаблона из библиотеки

**Действие:** "📁 Загрузить из библиотеки"

**Процесс:**
1. Открывается модальное окно библиотеки шаблонов
2. Список шаблонов с превью
3. Поиск по названию (фильтрация в реальном времени)
4. Клик на "Использовать":
   - Содержимое шаблона загружается в поле "Шаблон промпта"
   - Модальное окно закрывается
   - Устанавливается связь с шаблоном (templateFilename)

### 8.4 Сохранение шаблона в библиотеку

**Действие:** "💾 Сохранить в библиотеку"

**Процесс:**
1. Открывается модальное окно "Сохранить как шаблон"
2. Поле "Название шаблона" (автозаполнение: название промпта)
3. Readonly поле с содержимым (текущий шаблон промпта)
4. Кнопка "Сохранить":
   - Шаблон добавляется в личную библиотеку пользователя
   - Уведомление: "Шаблон '[Название]' сохранен в библиотеку"
   - Модальное окно закрывается

### 8.5 Выбор контекста для промпта

**Секция "Выбор контекста" в редакторе промпта:**

**Структура:**
- Древовидный список всех контекстных блоков проекта
- Каждый уровень имеет чекбокс:
  - **Блок:** выбрать весь блок целиком
  - **Элемент:** выбрать конкретный элемент
  - **Подэлемент:** выбрать конкретный подэлемент

**Логика выбора:**
- Чекбоксы независимы (можно выбрать подэлемент без выбора родительского элемента)
- Состояние чекбоксов сохраняется в массиве `selectedContexts` промпта
- Формат:
  ```
  selectedContexts: [
    { blockId: 1, itemIds: [1, 3], subItemIds: ['2.1', '3.2'] }
  ]
  ```

**Счетчик символов:**
- Под деревом: "Выбрано символов: XXXX"
- Подсчет суммы всех выбранных элементов и подэлементов
- Обновление в реальном времени при изменении чекбоксов

### 8.6 Компиляция промпта

**Действие:** Кнопка "Скомпилировать" или "Скомпилировать с тегами"

**Алгоритм компиляции:**

**Без тегов ("Скомпилировать"):**
1. Берется шаблон промпта
2. Собирается содержимое всех выбранных контекстов:
   - Элементы: их содержимое
   - Подэлементы: их содержимое
3. Все части объединяются через двойной перенос строки `\n\n`
4. Плейсхолдер `{{context}}` заменяется на собранное содержимое
5. Результат отображается в readonly поле "Результат"

**С тегами ("Скомпилировать с тегами"):**
1. Берется шаблон промпта
2. Собирается содержимое с XML обертками:
   ```
   <Context>
     <Блок контекста>
       <Элемент>
       содержимое элемента
       </Элемент>

       <Подэлемент>
       содержимое подэлемента
       </Подэлемент>
     </Блок контекста>
   </Context>
   ```
3. Плейсхолдер `{{context}}` заменяется на обернутое содержимое
4. Результат отображается в readonly поле "Результат"

**Автоматическая компиляция:**
- При изменении шаблона
- При изменении выбранных контекстов
- При изменении содержимого выбранных контекстов

### 8.7 Копирование результата

**Действие:** "📋 Копировать результат"

**Процесс:**
1. Скомпилированный промпт копируется в буфер обмена браузера (Clipboard API)
2. Уведомление: "Промпт скопирован в буфер обмена"
3. Пользователь может вставить в любой внешний чат-бот (ChatGPT, Claude и т.д.)

### 8.8 Отправка промпта через API

**Условие:** Настроен хотя бы один AI провайдер (API ключ добавлен и протестирован)

**Секция "Отправка в AI":**

**Элементы интерфейса:**
- **Выпадающий список моделей:**
  - Загружаются доступные модели из всех настроенных провайдеров
  - Группировка по провайдерам:
    ```
    OpenAI
      - GPT-4
      - GPT-4 Turbo
      - GPT-3.5 Turbo
    Anthropic
      - Claude 3 Opus
      - Claude 3 Sonnet
      - Claude 3 Haiku
    ...
    ```
  - Выбор модели сохраняется для текущего промпта

- **Кнопка "Отправить":**
  - Активна только если выбрана модель
  - При клике:
    1. Компилируется промпт (с учетом выбранного режима: с тегами или без)
    2. Формируется запрос к API выбранного провайдера
    3. Отправка через backend (для безопасности API ключей)
    4. Индикатор загрузки: "Отправка..." / "Ожидание ответа..."
    5. При получении ответа:
       - Отображается в поле "Ответ AI"
       - Возможность копирования ответа
    6. При ошибке:
       - Уведомление с описанием ошибки
       - Логирование ошибки

- **Поле "Ответ AI" (readonly):**
  - Отображает полученный ответ от модели
  - Кнопка "📋 Копировать ответ"
  - История ответов (опционально: сохранять последний ответ)

**Безопасность:**
- API ключи НЕ передаются на frontend
- Запросы к AI API проходят через backend
- Backend использует зашифрованные ключи пользователя из БД

### 8.9 Удаление промпта

**Действие:** Клик на "🗑️" в карточке промпта

**Процесс:**
1. Модальное окно подтверждения: "Удалить промпт '[Название]'?"
2. При подтверждении:
   - Удаление из БД
   - Удаление из списка
   - Если был открыт — очистка правой панели

---

## 9. AI ИНТЕГРАЦИЯ

### 9.1 Поддерживаемые провайдеры

**Список провайдеров:**
1. **OpenAI** — GPT-4, GPT-3.5 и другие модели
2. **Anthropic** — Claude 3 (Opus, Sonnet, Haiku)
3. **Google Gemini** — Gemini Pro, Ultra
4. **X.AI (Grok)** — Grok модели
5. **OpenRouter** — агрегатор 100+ моделей

### 9.2 Настройка API ключей

**Доступ:** Профиль → "API Ключи" или Header → кнопка "AI"

**Интерфейс настройки:**

**Для каждого провайдера:**
- Название провайдера
- Поле для ввода API ключа (тип: password, с кнопкой показать/скрыть)
- Кнопка "Тест подключения"
- Статус:
  - 🔴 **Не настроено** — ключ не добавлен
  - 🟡 **Проверка...** — идет тест подключения
  - 🟢 **Активно** — ключ валиден, модели загружены
  - 🔴 **Ошибка** — ключ невалиден или проблема с подключением
- Количество доступных моделей (если статус "Активно")

**Действия:**

**Добавление ключа:**
1. Пользователь вставляет API ключ в поле
2. Клик на "Сохранить" (или автосохранение при потере фокуса)
3. Ключ шифруется и сохраняется в БД
4. Автоматический запуск теста подключения

**Тест подключения:**
1. Backend отправляет тестовый запрос к API провайдера
2. При успехе:
   - Статус → "Активно"
   - Загружаются доступные модели
   - Модели добавляются в выпадающий список при отправке промптов
3. При ошибке:
   - Статус → "Ошибка"
   - Отображается описание ошибки (неверный ключ, нет доступа и т.д.)

**Обновление ключа:**
1. Пользователь изменяет ключ в поле
2. Автоматический запуск теста подключения
3. Обновление статуса

**Удаление ключа:**
1. Пользователь очищает поле
2. Ключ удаляется из БД
3. Статус → "Не настроено"
4. Модели этого провайдера убираются из списка выбора

### 9.3 Загрузка списка моделей

**Автоматическая загрузка:**
- При успешном добавлении/тесте API ключа
- Backend запрашивает список доступных моделей у провайдера
- Модели кэшируются на 24 часа (для снижения запросов к API)

**Ручное обновление:**
- Кнопка "🔄 Обновить модели" рядом с каждым провайдером
- Принудительная загрузка свежего списка моделей

**Хранение:**
- Списки моделей сохраняются в БД (для кэширования)
- TTL: 24 часа
- При истечении TTL — автообновление при следующем обращении

### 9.4 Отправка запроса к AI

**Workflow:**

**Frontend:**
1. Пользователь выбирает модель из списка
2. Клик "Отправить"
3. Компиляция промпта
4. Отправка POST запроса на backend:
   ```
   POST /api/ai/send
   {
     "promptId": "id_промпта",
     "modelId": "openai/gpt-4",
     "compiledPrompt": "текст_промпта",
     "withTags": false
   }
   ```

**Backend:**
1. Аутентификация пользователя (проверка JWT токена)
2. Получение зашифрованного API ключа для провайдера из БД
3. Расшифровка ключа
4. Определение провайдера по modelId
5. Формирование запроса к API провайдера:
   - Для OpenAI: Chat Completions API
   - Для Anthropic: Messages API
   - Для Gemini: generateContent API
   - И т.д.
6. Отправка запроса к AI API
7. Обработка ответа или ошибки
8. Возврат результата на frontend

**Frontend (получение ответа):**
1. Отображение ответа в поле "Ответ AI"
2. Логирование запроса/ответа (опционально, для истории)
3. Возможность копирования ответа

**Обработка ошибок:**
- Неверный API ключ → "Ошибка аутентификации. Проверьте API ключ."
- Превышен лимит токенов → "Промпт слишком длинный. Уменьшите количество контекста."
- Нет доступа к модели → "Модель недоступна. Проверьте подписку."
- Сетевая ошибка → "Ошибка подключения. Попробуйте позже."
- Другие ошибки → отображение сообщения от API

### 9.5 Streaming режим (опционально для будущих версий)

**Описание:** Постепенное отображение ответа AI по мере его генерации

**Текущая версия:** Обычный режим (получение полного ответа после завершения генерации)

---

## 10. ИНТЕРНАЦИОНАЛИЗАЦИЯ

### 10.1 Поддерживаемые языки

**В первой версии:**
- Английский (en) — язык по умолчанию
- Русский (ru)

**Архитектура:**
- Готовность к добавлению дополнительных языков
- Структура переводов: `src/locales/{lang}/`

### 10.2 Переключение языка

**Доступ:** Header → выпадающий список языков

**Процесс:**
1. Пользователь выбирает язык из списка
2. Немедленное обновление всего интерфейса (без перезагрузки страницы)
3. Сохранение выбора:
   - В профиле пользователя (БД) — для всех устройств
   - В localStorage браузера — для быстрого доступа
4. При следующем входе — автоматическая загрузка сохраненного языка

### 10.3 Определение языка при первом входе

**Алгоритм:**
1. Если пользователь новый (первая регистрация):
   - Определение языка браузера (`navigator.language`)
   - Если язык поддерживается (en/ru) → установка этого языка
   - Если язык не поддерживается → английский по умолчанию
2. Если пользователь существующий:
   - Загрузка сохраненного языка из профиля

### 10.4 Структура переводов

**Модули переводов:**
- `common.json` — общие элементы (кнопки, действия)
- `header.json` — верхняя панель
- `navigation.json` — левая панель
- `modals.json` — модальные окна
- `notifications.json` — уведомления
- `blocks.json` — контекстные блоки и промпты
- `editor.json` — редакторы
- `ai.json` — AI интеграция
- `profile.json` — профиль пользователя

**Формат:**
```json
{
  "buttons": {
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete"
  },
  "projects": {
    "new": "New Project",
    "limit": "Projects: {{count}} / {{max}}"
  }
}
```

### 10.5 Плейсхолдеры и интерполяция

**Использование переменных:**
- `{{count}}` — количество
- `{{name}}` — название
- `{{max}}` — максимум
- `{{date}}` — дата

**Примеры:**
- EN: "Projects: 5 / 10"
- RU: "Проекты: 5 / 10"

**Множественное число:**
- EN: "1 project" / "5 projects"
- RU: "1 проект" / "2 проекта" / "5 проектов"

---

## 11. ТЕМЫ ОФОРМЛЕНИЯ

### 11.1 Поддерживаемые темы

**В первой версии:**
- **Темная тема (Dark)** — по умолчанию
- Архитектура позволяет добавление светлой темы

**Будущие версии:**
- Светлая тема (Light)
- Возможность автоматического переключения по времени суток

### 11.2 Переключение темы

**Доступ:** Профиль → Настройки → "Тема"

**Процесс:**
1. Переключатель Dark / Light
2. Немедленное применение (без перезагрузки)
3. Сохранение в профиле пользователя (БД)
4. Сохранение в localStorage для быстрого доступа

### 11.3 Цветовая схема темной темы

**Базовые цвета:**
- Фон основной: `#1a1a1a` (очень темный серый)
- Фон панелей: `#242424` (темный серый)
- Фон элементов: `#2d2d2d` (средне-темный серый)
- Текст основной: `#e0e0e0` (светло-серый)
- Текст вторичный: `#a0a0a0` (серый)
- Акцентный цвет: `#4a9eff` (синий)
- Цвет успеха: `#10b981` (зеленый)
- Цвет ошибки: `#ef4444` (красный)
- Цвет предупреждения: `#f59e0b` (оранжевый)

**Границы и разделители:**
- Границы: `#3a3a3a` (темно-серый)
- Разделители: `#2a2a2a` (очень темно-серый)

---

## 12. ПОЛЬЗОВАТЕЛЬСКИЙ ОПЫТ (USER JOURNEY)

### 12.1 Первое посещение (новый пользователь)

**Шаг 1: Посадочная страница**
- Пользователь открывает сайт 
- Видит краткое описание сервиса
- Призыв к действию: "Войти через Google"

**Шаг 2: Авторизация**
- Клик на "Войти через Google"
- Редирект на Google OAuth
- Выбор аккаунта Google
- Разрешение доступа к email и профилю
- Редирект обратно в приложение

**Шаг 3: Создание аккаунта**
- Система автоматически создает аккаунт
- Определяется язык браузера → установка языка интерфейса
- Устанавливается темная тема

**Шаг 4: Приветственный экран**
- Пользователь попадает в пустой интерфейс
- Показывается Welcome модальное окно:
  - "Добро пожаловать в PromptyFlow!"
  - Краткое описание основных функций
  - Кнопка "Начать" или "Создать первый проект"

**Шаг 5: Создание первого проекта**
- Клик на "Создать первый проект"
- Модальное окно "Новый проект"
- Поле "Название проекта": placeholder "Мой первый проект"
- Кнопка "Создать"

**Шаг 6: Знакомство с интерфейсом**
- Проект создан и открыт
- Пустые панели с подсказками:
  - Центральная панель (вкладка "Контекст"): "Создайте блок контекста для организации информации"
  - Кнопка "+ Добавить блок контекста"
- Опциональный туториал (подсветка элементов интерфейса)

**Шаг 7: Создание первого контекста**
- Клик на "+ Добавить блок контекста"
- Создается блок "Новый контекст 1"
- Клик на "+ Добавить элемент"
- Создается элемент "Новый элемент 1"
- Правая панель открывает редактор
- Пользователь вводит название и содержимое
- Автосохранение

**Шаг 8: Создание первого промпта**
- Переход на вкладку "Промпт"
- Клик на "+ Добавить промпт"
- Создается "Новый промпт 1"
- Правая панель показывает редактор промпта
- Пользователь вводит шаблон с плейсхолдером `{{context}}`
- Выбирает созданный ранее контекст (чекбокс)
- Клик "Скомпилировать"
- Видит результат
- Клик "📋 Копировать результат"
- Уведомление: "Промпт скопирован в буфер обмена"

**Шаг 9: Первое использование**
- Пользователь вставляет скопированный промпт в ChatGPT/Claude
- Получает результат
- Понимает ценность сервиса

### 12.2 Повторное посещение (существующий пользователь)

**Шаг 1: Вход**
- Пользователь открывает сайт
- Автоматический вход через Google (если сессия активна)
- Или клик "Войти через Google" → быстрая авторизация

**Шаг 2: Загрузка данных**
- Восстанавливается последнее состояние:
  - Последний открытый проект
  - Активные вкладки
  - Позиция скролла
- Загружаются все проекты пользователя

**Шаг 3: Продолжение работы**
- Пользователь видит знакомый интерфейс
- Продолжает работу с того места, где остановился
- Или создает новый проект
- Или открывает другой существующий проект

### 12.3 Типичный рабочий процесс (опытный пользователь)

**Сценарий: Создание промпта для анализа документации**

**Этап 1: Подготовка контекста**
1. Открывает проект "Документация API"
2. Вкладка "Контекст" → "+ Добавить блок контекста"
3. Называет блок "API Методы"
4. Добавляет элемент "Метод GET /users"
5. Вставляет документацию метода в содержимое
6. Счетчик символов: 1500
7. Повторяет для других методов (POST, PUT, DELETE)

**Этап 2: Разделение больших текстов**
1. Добавляет элемент "Примеры использования"
2. Вставляет большой текст (5000 символов)
3. Клик "Разделить текст"
4. Выбирает "По параграфам"
5. Превью показывает: будет создано 8 подэлементов
6. Клик "Применить"
7. Текст автоматически разделен на подэлементы

**Этап 3: Создание промпта**
1. Переход на вкладку "Промпт"
2. "+ Добавить промпт"
3. Название: "Анализ API методов"
4. Клик "📁 Загрузить из библиотеки"
5. Выбирает шаблон "Технический анализ"
6. Шаблон загружен:
   ```
   Проанализируй следующую техническую документацию:

   {{context}}

   Ответь на вопросы:
   1. Какие методы представлены?
   2. Какие параметры они принимают?
   3. Какие ответы возвращают?
   ```

**Этап 4: Выбор контекста**
1. В секции "Выбор контекста" раскрывает блок "API Методы"
2. Отмечает чекбоксы:
   - Элемент "Метод GET /users"
   - Элемент "Метод POST /users"
   - Подэлемент "Пример 1" (из "Примеры использования")
3. Счетчик показывает: "Выбрано символов: 3200"

**Этап 5: Компиляция и отправка**
1. Клик "Скомпилировать с тегами"
2. Видит результат с XML структурой
3. Секция "Отправка в AI":
   - Выбирает модель "Anthropic / Claude 3 Opus"
   - Клик "Отправить"
   - Индикатор: "Ожидание ответа..."
   - Через 5 секунд получает развернутый анализ
4. Читает ответ, копирует нужные части

**Этап 6: Вариация промпта**
1. Дублирует промпт (Ctrl+D или контекстное меню)
2. Название: "Анализ API методов (кратко)"
3. Редактирует шаблон: добавляет "Ответь кратко, до 100 слов"
4. Снимает чекбокс с подэлемента "Пример 1"
5. Компилирует и отправляет
6. Получает краткую версию анализа

**Этап 7: Сохранение**
- Все изменения автоматически сохранены
- Индикатор в Header: "Сохранено"

### 12.4 Настройка AI интеграции

**Сценарий: Пользователь хочет отправлять промпты через API**

**Этап 1: Открытие настроек**
1. Клик на кнопку "AI" в Header
2. Или: Клик на аватар → "API Ключи"

**Этап 2: Добавление ключа OpenAI**
1. Видит список провайдеров
2. OpenAI — статус: 🔴 Не настроено
3. Поле "API Ключ": вставляет ключ `sk-...`
4. Клик "Сохранить" или автосохранение
5. Автоматически запускается тест
6. Статус меняется на 🟡 Проверка...
7. Через 2 секунды: 🟢 Активно
8. Под статусом: "Доступно моделей: 8"

**Этап 3: Добавление ключа Anthropic**
1. Вставляет ключ в поле Anthropic
2. Тест → 🟢 Активно
3. "Доступно моделей: 5"

**Этап 4: Ошибка с ключом Gemini**
1. Вставляет неверный ключ
2. Тест → 🔴 Ошибка
3. Сообщение: "Неверный API ключ. Проверьте правильность ключа."
4. Исправляет ключ
5. Повторный тест → 🟢 Активно

**Этап 5: Использование**
1. Закрывает модальное окно настроек
2. Открывает любой промпт
3. В секции "Отправка в AI" видит объединенный список моделей:
   ```
   OpenAI
     - GPT-4 Turbo
     - GPT-3.5 Turbo
   Anthropic
     - Claude 3 Opus
     - Claude 3 Sonnet
   Google Gemini
     - Gemini Pro
   ```
4. Выбирает модель и отправляет промпт

### 12.5 Работа с библиотекой шаблонов

**Сценарий: Создание и использование личной библиотеки**

**Этап 1: Создание шаблона**
1. Редактирует промпт с хорошим шаблоном
2. Клик "💾 Сохранить в библиотеку"
3. Модальное окно "Сохранить как шаблон"
4. Поле "Название": "Анализ текста для блога"
5. Клик "Сохранить"
6. Уведомление: "Шаблон сохранен в библиотеку"

**Этап 2: Просмотр библиотеки**
1. Header → кнопка "Библиотека"
2. Модальное окно со списком шаблонов
3. Видит созданный шаблон + любые другие

**Этап 3: Использование шаблона**
1. Создает новый промпт в другом проекте
2. Клик "📁 Загрузить из библиотеки"
3. Поиск: вводит "анализ"
4. Список фильтруется → видит свой шаблон
5. Клик "Использовать"
6. Шаблон загружен в поле промпта

**Этап 4: Редактирование шаблона**
1. В библиотеке: клик "✏️" на шаблоне
2. Редактирует содержимое
3. Клик "Сохранить"
4. Все промпты, связанные с этим шаблоном, обновятся (или нет, в зависимости от реализации связи)

**Этап 5: Удаление шаблона**
1. Клик "🗑️" на шаблоне
2. Подтверждение: "Удалить шаблон 'Анализ текста для блога'?"
3. Клик "Удалить"
4. Шаблон удален из библиотеки

### 12.6 Управление проектами

**Сценарий: Организация множества проектов**

**Этап 1: Создание проектов по темам**
1. Создает проект "Маркетинг"
2. Создает проект "Разработка"
3. Создает проект "HR"
4. ...и так до 10 проектов

**Этап 2: Попытка создать 11-й проект**
1. Клик "+ Новый проект"
2. Уведомление: "Достигнут лимит проектов для бесплатного плана (10). Удалите один из существующих проектов."
3. Кнопка создания неактивна

**Этап 3: Удаление старого проекта**
1. Находит неактуальный проект в списке
2. Контекстное меню → "Удалить"
3. Подтверждение
4. Проект удален
5. Счетчик: 9 / 10 проектов

**Этап 4: Дублирование проекта**
1. Проект "Маркетинг" — хорошая структура
2. Контекстное меню → "Дублировать"
3. Создается "Копия Маркетинг"
4. Переименовывает в "Маркетинг Q2"
5. Редактирует контент под новый квартал

**Этап 5: Экспорт для бэкапа**
1. Контекстное меню → "Экспорт в JSON"
2. Файл `marketing.json` скачан на компьютер
3. Сохраняет в облачном хранилище для бэкапа

**Этап 6: Импорт из бэкапа**
1. На другом компьютере: логин в PromptyFlow
2. Клик "Импорт проекта"
3. Выбирает файл `marketing.json`
4. Проект восстановлен со всем содержимым

### 12.7 Работа на разных устройствах

**Сценарий: Пользователь работает с ноутбука и планшета**

**На ноутбуке (утро):**
1. Открывает PromptyFlow
2. Создает проект "Статья о AI"
3. Добавляет контекстные блоки с исследованиями
4. Создает промпт для введения
5. Закрывает браузер

**На планшете (вечер):**
1. Открывает PromptyFlow в Safari
2. Автоматический вход через Google
3. Видит проект "Статья о AI" в списке
4. Открывает проект — все данные синхронизированы
5. Продолжает работу:
   - Добавляет новые контексты
   - Создает промпт для заключения
6. Компилирует и копирует

**На ноутбуке (следующий день):**
1. Открывает PromptyFlow
2. Проект автоматически обновлен со всеми вчерашними изменениями
3. Видит новые промпты, созданные на планшете

### 12.8 Переключение языка интерфейса

**Сценарий: Русскоязычный пользователь переключается на английский**

**Начальное состояние:**
- Интерфейс на русском
- Проекты с русскими названиями

**Действия:**
1. Header → выпадающий список "RU"
2. Выбирает "EN"
3. Мгновенное обновление всего интерфейса:
   - Кнопки: "Новый проект" → "New Project"
   - Вкладки: "Контекст" → "Context", "Промпт" → "Prompt"
   - Уведомления на английском
4. Пользовательский контент (названия проектов, контекст, промпты) остается на русском
5. Выбор сохранен в профиле

**На другом устройстве:**
- Логин → интерфейс сразу на английском (синхронизация настроек)

### 12.9 Обработка ошибок

**Сценарий 1: Потеря интернет-соединения**
1. Пользователь редактирует контекст
2. Интернет отключается
3. Пользователь продолжает вводить текст
4. Попытка автосохранения → ошибка
5. Уведомление: "⚠️ Нет подключения. Изменения будут сохранены при восстановлении связи."
6. Данные сохраняются в localStorage
7. Интернет восстанавливается
8. Автоматическая синхронизация с сервером
9. Уведомление: "✅ Изменения синхронизированы"

**Сценарий 2: Ошибка API при отправке промпта**
1. Пользователь отправляет промпт через OpenAI
2. API возвращает ошибку: "Rate limit exceeded"
3. Уведомление: "❌ Превышен лимит запросов OpenAI. Попробуйте через несколько минут или используйте другую модель."
4. Пользователь выбирает модель Anthropic
5. Успешная отправка

**Сценарий 3: Попытка импорта поврежденного файла**
1. Клик "Импорт проекта"
2. Выбирает файл с невалидным JSON
3. Уведомление: "❌ Ошибка импорта: неверный формат файла. Убедитесь, что это экспортированный проект PromptyFlow."
4. Файл не загружен, интерфейс остается стабильным

---

## 13. ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ

### 13.1 Frontend

**Технологии:**
- **React 18+** — UI библиотека
- **React Router** — маршрутизация (если требуется)
- **React Context / Redux Toolkit** — управление состоянием
- **i18next + react-i18next** — интернационализация
- **Tailwind CSS** — стилизация
- **Axios** — HTTP клиент
- **Zod** — валидация данных
- **React Query (TanStack Query)** — кэширование и синхронизация серверного состояния

**Структура:**
```
src/
├── components/        — React компоненты
│   ├── layout/        — Header, Layout, Panels
│   ├── projects/      — управление проектами
│   ├── context/       — контекстные блоки
│   ├── prompts/       — промпты
│   ├── library/       — библиотека шаблонов
│   ├── ai/            — AI интеграция
│   └── ui/            — общие UI компоненты
├── context/           — React Context провайдеры
├── hooks/             — кастомные хуки
├── services/          — API сервисы
├── utils/             — утилиты
├── locales/           — переводы (en/ru)
├── styles/            — глобальные стили
└── App.jsx            — корневой компонент
```

### 13.2 Backend

**Технологии:**
- **Node.js + Express** — веб-сервер
- **PostgreSQL** — реляционная БД для основных данных
- **Prisma / TypeORM** — ORM
- **Passport.js + Google OAuth 2.0** — аутентификация
- **JWT** — токены авторизации
- **bcrypt / crypto** — шифрование API ключей
- **Redis** — кэширование (списки моделей, сессии)
- **Winston / Pino** — логирование

**Структура:**
```
server/
├── routes/            — API маршруты
│   ├── auth.js        — авторизация
│   ├── projects.js    — проекты
│   ├── templates.js   — библиотека шаблонов
│   ├── ai.js          — AI интеграция
│   └── user.js        — профиль пользователя
├── controllers/       — контроллеры
├── models/            — модели данных (Prisma схемы)
├── services/          — бизнес-логика
│   ├── aiProviders/   — провайдеры AI (OpenAI, Anthropic и т.д.)
│   └── encryption.js  — шифрование ключей
├── middleware/        — middleware (auth, validation и т.д.)
├── utils/             — утилиты
└── server.js          — точка входа
```

### 13.3 База данных (PostgreSQL)

**Схема таблиц:**

**users:**
- id (UUID, PK)
- google_id (String, Unique)
- email (String, Unique)
- name (String)
- avatar_url (String)
- language (Enum: en/ru)
- theme (Enum: dark/light)
- created_at (Timestamp)
- updated_at (Timestamp)

**api_keys:**
- id (UUID, PK)
- user_id (UUID, FK → users.id)
- provider (Enum: openai, anthropic, gemini, grok, openrouter)
- encrypted_key (Text) — зашифрованный ключ
- status (Enum: not_configured, active, error)
- last_tested_at (Timestamp)
- created_at (Timestamp)
- updated_at (Timestamp)

**projects:**
- id (UUID, PK)
- user_id (UUID, FK → users.id)
- name (String)
- data (JSONB) — вся структура проекта (contextBlocks, promptBlocks)
- created_at (Timestamp)
- updated_at (Timestamp)

**templates:**
- id (UUID, PK)
- user_id (UUID, FK → users.id)
- name (String)
- content (Text)
- created_at (Timestamp)
- updated_at (Timestamp)

**models_cache:**
- id (UUID, PK)
- provider (Enum)
- models_list (JSONB)
- cached_at (Timestamp)
- expires_at (Timestamp)

**Индексы:**
- users.google_id
- users.email
- projects.user_id
- templates.user_id
- api_keys.user_id

### 13.4 API Endpoints

**Аутентификация:**
- `GET /auth/google` — инициация OAuth
- `GET /auth/google/callback` — OAuth callback
- `POST /auth/logout` — выход
- `GET /auth/me` — получение текущего пользователя

**Пользователь:**
- `GET /api/user/profile` — получение профиля
- `PATCH /api/user/profile` — обновление профиля (язык, тема)
- `GET /api/user/api-keys` — получение статусов API ключей
- `POST /api/user/api-keys/:provider` — добавление/обновление ключа
- `DELETE /api/user/api-keys/:provider` — удаление ключа
- `POST /api/user/api-keys/:provider/test` — тест подключения

**Проекты:**
- `GET /api/projects` — список всех проектов пользователя
- `GET /api/projects/:id` — получение проекта
- `POST /api/projects` — создание проекта
- `PATCH /api/projects/:id` — обновление проекта
- `DELETE /api/projects/:id` — удаление проекта
- `POST /api/projects/:id/duplicate` — дублирование проекта
- `POST /api/projects/import` — импорт проекта из JSON

**Библиотека шаблонов:**
- `GET /api/templates` — список шаблонов пользователя
- `GET /api/templates/:id` — получение шаблона
- `POST /api/templates` — создание шаблона
- `PATCH /api/templates/:id` — обновление шаблона
- `DELETE /api/templates/:id` — удаление шаблона

**AI интеграция:**
- `GET /api/ai/providers` — список провайдеров и их статусов
- `GET /api/ai/models` — список доступных моделей (из всех настроенных провайдеров)
- `POST /api/ai/models/refresh` — обновление списка моделей
- `POST /api/ai/send` — отправка промпта к AI

### 13.5 Безопасность

**Аутентификация:**
- Google OAuth 2.0 для входа
- JWT токены (httpOnly cookies) для сессий
- Refresh tokens для продления сессий

**Шифрование:**
- API ключи шифруются перед сохранением в БД (AES-256)
- Ключ шифрования хранится в переменных окружения
- Расшифровка только на backend при запросах к AI API

**Защита от атак:**
- Rate limiting на всех endpoints (express-rate-limit)
- CORS настроен только для фронтенд домена
- Helmet.js для защиты заголовков
- Валидация всех входных данных (Zod)
- Sanitization для предотвращения XSS

**Изоляция данных:**
- Каждый пользователь видит только свои данные
- Проверка user_id при всех операциях с БД
- Middleware для авторизации на всех защищенных роутах

### 13.6 Производительность

**Frontend:**
- Code splitting (React.lazy)
- Мемоизация компонентов (React.memo)
- Виртуализация длинных списков (react-window)
- Debounce для автосохранения
- localStorage для кэширования настроек UI

**Backend:**
- Redis кэш для списков моделей AI (TTL: 24 часа)
- Connection pooling для PostgreSQL
- Индексы на часто запрашиваемых полях
- Пагинация для больших списков (если понадобится)

**База данных:**
- JSONB для гибкого хранения структур проектов
- Partial indexes для оптимизации запросов

### 13.7 Мониторинг и логирование

**Логи:**
- Все запросы к AI API (для отладки и аналитики)
- Ошибки авторизации
- Критические ошибки сервера
- Ротация логов (Winston transports)

**Метрики:**
- Количество пользователей
- Количество созданных проектов
- Количество запросов к AI API (по провайдерам)
- Время ответа API endpoints

---

## 14. ОТЛИЧИЯ ОТ DESKTOP ВЕРСИИ

### 14.1 Что убрано

**Из desktop версии НЕ переносится:**
- ❌ Управление локальными файлами и папками
- ❌ Шаблоны проектов (пока, может быть добавлено позже)
- ❌ Экспорт контекстных блоков в отдельные файлы (теперь все в БД)
- ❌ Импорт .txt шаблонов из файловой системы
- ❌ Интеграция с системным хранилищем ключей (keytar)
- ❌ Проверка орфографии на уровне Electron (заменена браузерной)
- ❌ Горячие клавиши уровня приложения (F1, Ctrl+E) — могут быть добавлены позже

### 14.2 Что изменено

**Трансформации:**
- 📁 **Файлы → База данных:** все проекты, контексты, промпты хранятся в PostgreSQL
- 🔑 **Keytar → Зашифрованное хранилище:** API ключи шифруются и хранятся на сервере
- 👤 **Один пользователь → Многопользовательский:** каждый пользователь имеет изолированный аккаунт
- ♾️ **Неограниченно → Лимит 10 проектов:** ограничение бесплатного плана
- 💾 **Ручное сохранение → Автосохранение:** все изменения сохраняются автоматически в облаке
- 🌐 **Локальное приложение → Веб-сервис:** доступ с любого устройства через браузер

### 14.3 Что сохранено на 100%

**Ключевая функциональность:**
- ✅ Трехуровневая структура контекста (Блок → Элемент → Подэлемент)
- ✅ Компиляция промптов с плейсхолдером `{{context}}`
- ✅ Выбор контекстов через чекбоксы
- ✅ Счетчики символов на всех уровнях
- ✅ Разделение текста на части (по символам, параграфам, предложениям)
- ✅ Обертка контекста в XML теги
- ✅ Библиотека шаблонов промптов (теперь персональная в облаке)
- ✅ Интеграция с 5 AI провайдерами (OpenAI, Anthropic, Gemini, Grok, OpenRouter)
- ✅ Тестирование API ключей
- ✅ Загрузка списков моделей
- ✅ Отправка промптов через API
- ✅ Копирование результата в буфер обмена
- ✅ Двуязычный интерфейс (EN/RU)
- ✅ Темная тема
- ✅ Экспорт/импорт проектов в JSON

