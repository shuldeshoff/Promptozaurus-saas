# 📋 План доработок Promptozaurus

## 🔥 Критичные исправления (1-2 дня)

### 1. Баг с разрезанием текста (2-3 часа)
**Проблема:**
- При разрезании блоков контекста первый элемент теряется
- Исходный текст сохраняется вместо первого элемента

**Решение:**
- Исследовать логику `splitBlock` в компонентах работы с контекстом
- Исправить сохранение первого элемента после разрезания

**Файлы:**
- `apps/web/src/components/context/*`
- API endpoint для split операций

---

### 2. Лимиты - проверка и исправление (1-2 часа)
**Проблема:**
- Показывается предупреждение при 500K вместо 5M/10M
- Непонятна логика: лимит блока vs лимит проекта

**Решение:**
- Проверить `apps/api/src/services/project.service.ts` (validateProjectSize)
- Проверить `apps/web/src/hooks/useProjectUpdate.ts`
- Уточнить и документировать лимиты:
  - Блок контекста: 5M символов
  - Проект целиком: 10M символов

**Файлы:**
- `apps/api/src/services/project.service.ts`
- `apps/web/src/hooks/useProjectUpdate.ts`

---

## ⚡ Быстрые UX улучшения (1 день)

### 3. Автоназвание модели (30 минут)
**Проблема:**
- Неудобно каждый раз придумывать название при добавлении модели

**Решение:**
- По умолчанию подставлять название модели из списка
- Оставить возможность редактирования
- Формат: `{provider} {modelName}` или просто `{modelName}`

**Файлы:**
- `apps/web/src/components/AIConfigModal.tsx`

---

### 4. Переработка карточек проектов (1-2 часа)
**Текущий формат:**
- Смешанное отображение информации

**Новый формат (3 строки):**
```
Название проекта
05.12.2025 • 125.5K
2 contexts × 3 prompts
```

**Файлы:**
- `apps/web/src/components/ProjectList.tsx`
- Возможно CSS/Tailwind стили

---

### 5. Доработка Help/FAQ (1-2 часа)
**Добавить:**
- Подробное описание лимитов (блок vs проект)
- Примеры использования
- Рекомендации по работе с API ключами
- Описание всех провайдеров
- Troubleshooting секция

**Файлы:**
- `apps/web/src/components/HelpModal.tsx` (создать если нет)
- Или `apps/web/src/pages/Help.tsx`

---

## 🚀 Новый функционал (3-5 дней)

### 6. Система тегов и наборов (2-3 дня)

#### 6.1 Теги для промптов
**Функционал:**
- Добавление/удаление тегов к промптам
- Фильтрация промптов по тегам
- Autocomplete при вводе тегов
- Цветовая кодировка тегов

**БД изменения:**
```sql
CREATE TABLE tags (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  name VARCHAR(50) NOT NULL,
  color VARCHAR(7),
  created_at TIMESTAMP
);

CREATE TABLE template_tags (
  template_id UUID,
  tag_id UUID,
  PRIMARY KEY (template_id, tag_id)
);
```

#### 6.2 Наборы промптов (коллекции)
**Функционал:**
- Создание именованных наборов
- Добавление промптов в наборы
- Один промпт может быть в нескольких наборах
- Связь набора с проектом

**БД изменения:**
```sql
CREATE TABLE prompt_collections (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,
  name VARCHAR(100) NOT NULL,
  description TEXT,
  created_at TIMESTAMP
);

CREATE TABLE collection_templates (
  collection_id UUID,
  template_id UUID,
  order_index INT,
  PRIMARY KEY (collection_id, template_id)
);
```

**Файлы:**
- `apps/api/src/services/tag.service.ts` (новый)
- `apps/api/src/services/collection.service.ts` (новый)
- `apps/api/src/routes/tag.routes.ts` (новый)
- `apps/api/src/routes/collection.routes.ts` (новый)
- `apps/web/src/components/TagManager.tsx` (новый)
- `apps/web/src/components/CollectionManager.tsx` (новый)

---

### 7. Экспорт/импорт (1-2 дня)

#### 7.1 Экспорт
**Форматы:**
- Проект целиком (JSON)
- Набор промптов (JSON)
- Отдельный промпт (JSON/Markdown)

**Структура JSON:**
```json
{
  "version": "1.0",
  "type": "project|collection|template",
  "data": {
    "name": "...",
    "contexts": [...],
    "prompts": [...],
    "tags": [...]
  }
}
```

#### 7.2 Импорт
**Функционал:**
- Валидация формата
- Проверка на дубликаты
- Опция: merge или replace
- Обработка конфликтов тегов

**Файлы:**
- `apps/api/src/services/export.service.ts` (новый)
- `apps/api/src/services/import.service.ts` (новый)
- `apps/web/src/components/ExportModal.tsx` (новый)
- `apps/web/src/components/ImportModal.tsx` (новый)

---

## 📚 Документация и тесты (3-4 дня)

### 8. Тесты (2 дня)

#### 8.1 Backend тесты
**Unit тесты:**
- `*.provider.ts` - все AI провайдеры
- `*.service.ts` - все сервисы
- Утилиты и helpers

**Integration тесты:**
- API endpoints
- БД операции
- Auth flow

**Покрытие:** минимум 60%

**Инструменты:**
- Jest
- Supertest для API

**Файлы:**
- `apps/api/src/**/*.test.ts`
- `apps/api/jest.config.js`

#### 8.2 Frontend тесты
**Unit тесты:**
- Компоненты
- Hooks
- Утилиты

**E2E тесты:**
- Создание проекта
- Работа с промптами
- Отправка в AI
- Сохранение ответов

**Инструменты:**
- Vitest / Jest
- React Testing Library
- Playwright для E2E

**Файлы:**
- `apps/web/src/**/*.test.tsx`
- `apps/web/vitest.config.ts`
- `apps/web/e2e/**/*.spec.ts`

---

### 9. Документация проекта (1-2 дня)

#### 9.1 README.md
**Содержание:**
- Описание проекта
- Основные возможности
- Скриншоты/GIF
- Требования к системе
- Быстрый старт

#### 9.2 docs/ARCHITECTURE.md
**Содержание:**
- Общая архитектура (monorepo)
- Структура папок
- Технологический стек
- Паттерны и подходы
- Диаграммы компонентов

#### 9.3 docs/DATABASE.md
**Содержание:**
- ER-диаграмма
- Описание всех таблиц
- Индексы и оптимизации
- Миграции
- Seed данные

#### 9.4 docs/API.md
**Содержание:**
- Все endpoints с примерами
- Request/Response форматы
- Коды ошибок
- Аутентификация
- Rate limiting

#### 9.5 docs/DEVELOPMENT.md
**Содержание:**
- Настройка окружения
- Установка зависимостей
- Запуск в dev режиме
- Структура кода
- Code style и conventions
- Git workflow
- Debugging

#### 9.6 docs/DEPLOYMENT.md
**Содержание:**
- Требования к серверу
- Настройка БД
- Настройка Nginx
- SSL сертификаты
- Environment variables
- Процесс деплоя (пошагово)
- Rollback процедура
- Мониторинг и логи

---

## ⏱️ Общая оценка времени

| Категория | Задачи | Время |
|-----------|--------|-------|
| **Критичные исправления** | 1-2 | 1-2 дня |
| **UX улучшения** | 3-5 | 1 день |
| **Новый функционал** | 6-7 | 3-5 дней |
| **Тесты + документация** | 8-9 | 3-4 дня |
| **ИТОГО** | | **8-12 дней** |

---

## ❓ Вопросы для уточнения

### 1. Приоритеты для сдачи тестового
- Что критично сделать в первую очередь?
- Какой минимальный набор для сдачи?
- Дедлайн?

### 2. Теги и наборы - детальная спецификация
- Как должны выглядеть визуально?
- Где в интерфейсе размещаться?
- Drag & drop для управления?
- Права доступа (личные/общие наборы)?

### 3. Тесты
- Требуемое покрытие кода?
- Критичные сценарии для E2E?
- CI/CD pipeline нужен?

### 4. Доступ к коду
- GitHub приватный репозиторий?
- GitLab?
- Или zip-архив?
- Нужен ли CI/CD setup?

---

## 📝 Дополнительные предложения по оптимизации

### Общая библиотека промптов
**Идея:**
- Публичная библиотека шаблонов
- Категории (SEO, код, маркетинг, и т.д.)
- Рейтинги и отзывы
- Форк чужих промптов в свою коллекцию

**Оценка:** 5-7 дней

### Версионирование промптов
**Идея:**
- История изменений промпта
- Откат к предыдущей версии
- Diff между версиями
- Branching для экспериментов

**Оценка:** 3-4 дня

### Collaborative features
**Идея:**
- Шаринг проектов между пользователями
- Командная работа
- Комментарии к промптам
- Real-time collaboration

**Оценка:** 7-10 дней

### Analytics
**Идея:**
- Статистика использования моделей
- Стоимость запросов
- Популярные промпты
- A/B тестирование промптов

**Оценка:** 3-5 дней

---

**Дата создания:** 05.12.2025  
**Статус:** В планировании

