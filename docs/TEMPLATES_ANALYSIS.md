# 📋 Анализ системы работы с шаблонами промптов

**Дата:** 30 ноября 2025  
**Проект:** Promptozaurus SaaS

---

## ⚠️ Важное примечание: Миграция с файловой системы

### История изменений

**Оригинальный проект (Electron Desktop App):**
- Шаблоны хранились в **`.txt` файлах** на локальном диске
- Путь: `~/Documents/Promptozaurus/templates/*.txt`
- Использовался Node.js `fs` API через IPC Electron
- Файлы читались/записывались напрямую в файловую систему

**Текущий проект (SaaS Web App):**
- Шаблоны хранятся в **PostgreSQL базе данных**
- Таблица `templates` с полями `id`, `userId`, `name`, `content`
- REST API через Fastify backend
- Полная изоляция данных между пользователями

### Устаревшие упоминания `.txt`

В процессе миграции в локализации остались **устаревшие строки**:
- ❌ ~~"Нет файлов .txt"~~ → ✅ "Нет сохранённых шаблонов"
- ❌ ~~"Введите имя файла (без .txt)"~~ → ✅ "Введите название шаблона"
- ❌ ~~"Файл: Название"~~ → ✅ "Шаблон: Название"
- ❌ ~~"Указать директорию для txt–шаблонов"~~ → ✅ "Управление шаблонами в библиотеке"

**Статус:** ✅ Исправлено в коммите от 30.11.2025

---

## 🏗️ Архитектура хранения шаблонов

### 1. База данных (PostgreSQL + Prisma)

**Модель `Template`** (`apps/api/prisma/schema.prisma`):

```prisma
model Template {
  id String @id @default(uuid())
  
  userId String @map("user_id")
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  name    String           // Имя шаблона (например: "Customer Support Reply")
  content String @db.Text  // Содержимое шаблона (текст промпта)
  
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@index([userId])
  @@map("templates")
}
```

**Ключевые особенности:**
- ✅ Каждый шаблон привязан к пользователю (`userId`)
- ✅ Автоматическое удаление при удалении пользователя (`onDelete: Cascade`)
- ✅ Индекс по `userId` для быстрого поиска
- ✅ Хранится полный текст промпта в поле `content`
- ✅ Автоматическое отслеживание времени создания/обновления

---

## 🔄 Связь с проектами

### 2. Модель `Project` и связь с шаблонами

**Структура проекта:**
```typescript
// Проект хранит данные в JSONB поле "data"
interface Project {
  id: string;
  userId: string;
  name: string;
  data: {
    promptBlocks: PromptBlock[];  // Массив блоков промптов
    contextBlocks: ContextBlock[];
  };
}

interface PromptBlock {
  id: number;
  title: string;
  template: string;              // ТЕКСТ шаблона (копия)
  templateFilename?: string;      // ID шаблона из таблицы templates
  selectedContexts: SelectedContext[];
  wrapWithTags: boolean;
}
```

**⚠️ ВАЖНО: Двойное хранение данных**

1. **В проекте (`promptBlock.template`):**
   - Хранится полная копия текста шаблона
   - Пользователь редактирует напрямую
   - Сохраняется автоматически при изменении

2. **В таблице templates (`templateFilename`):**
   - Хранится только ID ссылки на шаблон
   - Используется для:
     - Определения "связанного" шаблона
     - Кнопки "Сохранить шаблон" (обновление существующего)
     - Кнопки "Сохранить как" (создание нового)

---

## 🛠️ Backend API (Fastify)

### 3. Сервис для работы с шаблонами

**Файл:** `apps/api/src/services/template.service.ts`

**Методы:**

#### 3.1. `getUserTemplates(userId: string)`
- Получить все шаблоны пользователя
- Сортировка: по `updatedAt DESC` (новые сверху)

#### 3.2. `getTemplateById(templateId: string, userId: string)`
- Получить конкретный шаблон по ID
- Проверка прав доступа (только свои шаблоны)

#### 3.3. `createTemplate(input: CreateTemplateInput)`
- Создать новый шаблон
- Параметры: `name`, `content`, `userId`
- Возвращает созданный объект `Template`

#### 3.4. `updateTemplate(templateId: string, userId: string, input: UpdateTemplateInput)`
- Обновить существующий шаблон
- Параметры: `name?`, `content?`
- Проверка прав доступа

#### 3.5. `deleteTemplate(templateId: string, userId: string)`
- Удалить шаблон
- Проверка прав доступа

#### 3.6. `searchTemplates(userId: string, query: string)`
- Поиск по имени или содержимому
- Case-insensitive поиск по `name` и `content`

#### 3.7. `getTemplateCount(userId: string)`
- Количество шаблонов пользователя

---

### 4. API Endpoints

**Файл:** `apps/api/src/routes/template.routes.ts`

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|----------|-------------|
| GET | `/api/templates` | Список всех шаблонов | ✅ Требуется |
| GET | `/api/templates/:id` | Получить шаблон по ID | ✅ Требуется |
| GET | `/api/templates/search?q=...` | Поиск шаблонов | ✅ Требуется |
| POST | `/api/templates` | Создать шаблон | ✅ Требуется |
| PATCH | `/api/templates/:id` | Обновить шаблон | ✅ Требуется |
| DELETE | `/api/templates/:id` | Удалить шаблон | ✅ Требуется |
| GET | `/api/templates/stats/count` | Количество шаблонов | ✅ Требуется |

**Пример запроса:**
```bash
# Получить все шаблоны
curl -H "Authorization: Bearer <JWT>" https://promptyflow.com/api/templates

# Создать новый шаблон
curl -X POST \
  -H "Authorization: Bearer <JWT>" \
  -H "Content-Type: application/json" \
  -d '{"name": "Support Reply", "content": "Dear customer..."}' \
  https://promptyflow.com/api/templates

# Поиск
curl -H "Authorization: Bearer <JWT>" \
  "https://promptyflow.com/api/templates/search?q=customer"
```

---

## 🎨 Frontend (React)

### 5. React Hooks для работы с шаблонами

**Файл:** `apps/web/src/hooks/useTemplates.ts`

#### 5.1. `useTemplates()`
```typescript
const { data: templates, isLoading } = useTemplates();
// Возвращает: Template[] | undefined
```
- Загружает все шаблоны пользователя
- Автоматическая инвалидация кеша при изменениях
- React Query кеширование

#### 5.2. `useTemplate(templateId: string)`
```typescript
const { data: template } = useTemplate(templateId);
// Возвращает: Template | null
```
- Загружает содержимое конкретного шаблона
- Используется для preview в выпадающем меню

#### 5.3. `useCreateTemplate()`
```typescript
const createMutation = useCreateTemplate();
await createMutation.mutateAsync({ name, content });
```
- Создает новый шаблон
- Автоматическая инвалидация кеша

#### 5.4. `useUpdateTemplate()`
```typescript
const updateMutation = useUpdateTemplate();
await updateMutation.mutateAsync({ templateId, name, content });
```
- Обновляет существующий шаблон
- Автоматическая инвалидация кеша

#### 5.5. `useDeleteTemplate()`
```typescript
const deleteMutation = useDeleteTemplate();
await deleteMutation.mutateAsync(templateId);
```
- Удаляет шаблон
- Автоматическая инвалидация кеша

#### 5.6. `useSearchTemplates(query: string)`
```typescript
const { data: searchResults } = useSearchTemplates('customer');
```
- Серверный поиск по шаблонам
- Поиск по `name` и `content`

---

## 📝 Компоненты UI

### 6. PromptEditor - главный редактор промптов

**Файл:** `apps/web/src/components/prompt/PromptEditor.tsx`

#### 6.1. Состояние компонента

```typescript
const [isMenuOpen, setIsMenuOpen] = useState(false);           // Открыто ли меню шаблонов
const [previewTemplateId, setPreviewTemplateId] = useState<string | null>(null);  // ID для preview
const [previewContent, setPreviewContent] = useState<string | null>(null);        // Содержимое preview
const [searchQuery, setSearchQuery] = useState('');            // Поиск по шаблонам

const { data: templates = [], isLoading: isLoadingTemplates } = useTemplates();   // Все шаблоны
const { data: previewTemplateData } = useTemplate(previewTemplateId || '');      // Preview шаблона
```

#### 6.2. Определение текущего шаблона

```typescript
// Определяем ID привязанного шаблона
const currentTemplateId = block?.templateFilename;

// Находим объект шаблона по ID
const currentTemplate = useMemo(() => {
  if (!currentTemplateId || !templates || templates.length === 0) return null;
  return templates.find((t) => t.id === currentTemplateId) || null;
}, [currentTemplateId, templates]);
```

**Логика:**
- `block.templateFilename` содержит ID шаблона из таблицы `templates`
- Если ID есть → ищем шаблон в загруженном списке
- Если нашли → показываем "Файл: <имя шаблона>"
- Если не нашли → показываем "Выбрать шаблон"

---

### 7. Работа с UI шаблонов

#### 7.1. Кнопка "Выбрать шаблон"

**Расположение:** Над текстовым полем редактирования промпта

```tsx
<button
  onClick={toggleMenu}
  className="px-2 py-1 bg-purple-700 text-xs text-white rounded hover:bg-purple-600"
>
  {isLoadingTemplates
    ? t('prompt.template.loading')
    : currentTemplate
    ? `${t('prompt.template.file')}: ${currentTemplate.name}`
    : t('prompt.template.select')}
</button>
```

**Отображение:**
- Если шаблон загружается: `"Загрузка..."`
- Если шаблон привязан: `"Файл: Customer Support Reply"`
- Если шаблона нет: `"Выбрать шаблон"`

#### 7.2. Выпадающее меню шаблонов

**Структура меню:**
```
┌─────────────────────────────────────────────────────┐
│ 🔍 Поиск: [_________________]                      │
│                                                     │
│ Список шаблонов    │    Превью                     │
│ ───────────────────┼──────────────────────────     │
│ ☐ Template 1       │    Содержимое шаблона...     │
│ ☐ Template 2       │                              │
│ ☑️ Template 3       │                              │
│                    │                              │
│                    │                              │
└─────────────────────────────────────────────────────┘
```

**Функциональность:**
- Левая панель: список всех шаблонов пользователя
- Правая панель: превью содержимого при наведении
- Поиск: фильтрация по имени (клиентская)
- Выбор: клик на шаблон → загружается в промпт

#### 7.3. Выбор шаблона из меню

```typescript
const handleSelectTemplate = async (template: Template) => {
  if (!template || !currentProject || !block) return;

  try {
    // Обновляем блок промпта:
    // 1. template - копируем текст шаблона
    // 2. templateFilename - запоминаем ID шаблона
    const updatedBlocks = currentProject.data.promptBlocks.map((b) =>
      b.id === block.id
        ? { 
            ...b, 
            template: template.content,        // ← КОПИРУЕМ текст
            templateFilename: template.id      // ← ЗАПОМИНАЕМ ID
          }
        : b
    );

    await updateProjectAndRefresh({
      ...currentProject.data,
      promptBlocks: updatedBlocks,
    });

    // Закрываем меню
    setIsMenuOpen(false);
  } catch (error) {
    console.error('Error handleSelectTemplate:', error);
  }
};
```

**⚠️ Важно:**
- Текст шаблона **копируется** в `template`
- После копирования пользователь может редактировать текст
- Изменения в `template` НЕ влияют на исходный шаблон в базе
- ID `templateFilename` используется только для определения связи

---

### 8. Сохранение шаблонов

#### 8.1. Кнопка "Сохранить шаблон"

**Логика:**
```typescript
const handleSaveTemplateFile = () => {
  if (!block?.template) return;
  
  if (!currentTemplateId || !currentTemplate) {
    // Если нет привязанного шаблона → "Сохранить как" (создать новый)
    return handleSaveTemplateFileAs();
  }
  
  // Если есть привязанный шаблон → обновить существующий
  doUpdateTemplate(currentTemplateId, currentTemplate.name);
};
```

**Поведение:**
1. Если `templateFilename` пустой → открывается диалог "Сохранить как"
2. Если `templateFilename` задан → обновляется существующий шаблон

#### 8.2. "Сохранить как" (создать новый)

```typescript
const handleSaveTemplateFileAs = () => {
  if (!block?.template) return;
  
  // Открываем диалог подтверждения с вводом имени
  openConfirmation(
    t('prompt.saveTemplate.title'),
    t('prompt.saveTemplate.message'),
    async (templateName?: string) => {
      let name = templateName || '';
      if (!name.trim()) {
        name = block.title || 'my_template';  // По умолчанию: название блока
      }
      doCreateTemplate(name.trim());
    },
    t('prompt.buttons.save'),
    'bg-blue-600 hover:bg-blue-700',
    true,
    currentTemplate?.name || block.title  // Предзаполненное значение
  );
};
```

#### 8.3. Создание нового шаблона

```typescript
const doCreateTemplate = async (name: string) => {
  if (!currentProject || !block) return;
  
  try {
    // Создаем шаблон через API
    const newTemplate = await createTemplateMutation.mutateAsync({
      name,
      content: block.template || '',
    });

    // Обновляем проект: запоминаем ID созданного шаблона
    const updatedBlocks = currentProject.data.promptBlocks.map((b) =>
      b.id === block.id 
        ? { ...b, templateFilename: newTemplate.id }  // ← СВЯЗЫВАЕМ с шаблоном
        : b
    );

    await updateProjectAndRefresh({
      ...currentProject.data,
      promptBlocks: updatedBlocks,
    });

    setSaveSuccess(true);
    setTimeout(() => setSaveSuccess(false), 2000);
  } catch (err) {
    console.error('Error creating template:', err);
  }
};
```

**Что происходит:**
1. API создает новый шаблон в таблице `templates`
2. Возвращается объект с `id`
3. ID сохраняется в `block.templateFilename`
4. Теперь кнопка "Сохранить" будет обновлять этот шаблон

#### 8.4. Обновление существующего шаблона

```typescript
const doUpdateTemplate = async (templateId: string, name: string) => {
  if (!block) return;
  
  try {
    // Обновляем шаблон через API
    await updateTemplateMutation.mutateAsync({
      templateId,
      name,
      content: block.template || '',  // ← Берем текущий текст из редактора
    });

    setSaveSuccess(true);
    setTimeout(() => setSaveSuccess(false), 2000);
  } catch (err) {
    console.error('Error updating template:', err);
  }
};
```

**Что происходит:**
1. Берется текущий текст из `block.template`
2. API обновляет запись в таблице `templates`
3. Все другие проекты, использующие этот шаблон, будут видеть старую версию (пока не выберут его заново)

---

## 🔍 Дополнительные компоненты

### 9. TemplateLibraryModal - модальное окно библиотеки шаблонов

**Файл:** `apps/web/src/components/TemplateLibraryModal.tsx`

**Функции:**
- Просмотр всех шаблонов
- Создание нового шаблона
- Редактирование существующего
- Удаление шаблона
- Выбор шаблона для загрузки в промпт

**Состояние:**
```typescript
const [searchQuery, setSearchQuery] = useState('');
const [selectedTemplateId, setSelectedTemplateId] = useState<string | null>(null);
const [isEditing, setIsEditing] = useState(false);
const [isCreating, setIsCreating] = useState(false);
const [editName, setEditName] = useState('');
const [editContent, setEditContent] = useState('');
```

**Использование:**
```tsx
<TemplateLibraryModal
  isOpen={isLibraryOpen}
  onClose={() => setIsLibraryOpen(false)}
  onSelectTemplate={(template) => {
    // Загрузить выбранный шаблон в редактор
    handleSelectTemplate(template);
  }}
/>
```

---

## 📊 Диаграмма работы системы

```
┌─────────────────────────────────────────────────────────────────┐
│                         ПОЛЬЗОВАТЕЛЬ                            │
└─────────────────┬───────────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      ПРОМПТ РЕДАКТОР                            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Название: [Prompt1_________________________]            │   │
│  │                                                          │   │
│  │ [Выбрать шаблон ▼] 0 символов ✏️                        │   │
│  │                                                          │   │
│  │ Текстовая область:                                       │   │
│  │ ┌────────────────────────────────────────────────────┐   │   │
│  │ │ {context}                                          │   │   │
│  │ │                                                    │   │   │
│  │ │ Please analyze...                                  │   │   │
│  │ └────────────────────────────────────────────────────┘   │   │
│  │                                                          │   │
│  │ [☑️ Обернуть тегами]  [Шаблон: 0 | Контекст: 0...]    │   │
│  │                                                          │   │
│  │ [📋 Копировать шаблон] [💾 Сохранить шаблон]           │   │
│  │ [📋 Копировать полный] [🤖 Отправить ИИ]              │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────┬───────────────────────────────────────────────┘
                  │
    ┌─────────────┴──────────────┐
    │                            │
    ▼                            ▼
┌──────────────────┐     ┌───────────────────┐
│  React Hooks     │     │  EditorContext    │
│  useTemplates()  │     │  currentProject   │
│  useTemplate()   │     │  activePromptBlock│
│  useCreateTmpl() │     └───────────────────┘
│  useUpdateTmpl() │
│  useDeleteTmpl() │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│           API CLIENT (axios)                │
│  GET    /api/templates                      │
│  GET    /api/templates/:id                  │
│  POST   /api/templates                      │
│  PATCH  /api/templates/:id                  │
│  DELETE /api/templates/:id                  │
└────────┬────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│          BACKEND API (Fastify)              │
│  templateRoutes.ts                          │
│  ├─ authenticate middleware                 │
│  └─ templateService                         │
└────────┬────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│       TEMPLATE SERVICE                      │
│  templateService.ts                         │
│  ├─ getUserTemplates()                      │
│  ├─ getTemplateById()                       │
│  ├─ createTemplate()                        │
│  ├─ updateTemplate()                        │
│  ├─ deleteTemplate()                        │
│  └─ searchTemplates()                       │
└────────┬────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│         PRISMA ORM                          │
│  prisma.template                            │
└────────┬────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│      POSTGRESQL DATABASE                    │
│  Table: templates                           │
│  ├─ id (UUID)                               │
│  ├─ userId (UUID, FK)                       │
│  ├─ name (String)                           │
│  ├─ content (Text)                          │
│  ├─ createdAt (DateTime)                    │
│  └─ updatedAt (DateTime)                    │
└─────────────────────────────────────────────┘
```

---

## 🔄 Жизненный цикл шаблона

### Сценарий 1: Создание нового шаблона

```
1. Пользователь пишет промпт в редакторе
   └─> block.template = "Dear customer..."
   
2. Нажимает "Сохранить шаблон"
   └─> Открывается диалог "Введите имя"
   
3. Вводит имя: "Customer Support"
   └─> doCreateTemplate("Customer Support")
   
4. Frontend → Backend: POST /api/templates
   Body: { name: "Customer Support", content: "Dear customer..." }
   
5. Backend → Database: INSERT INTO templates
   └─> Возвращает: { id: "uuid-123", name: "Customer Support", ... }
   
6. Frontend обновляет проект:
   └─> block.templateFilename = "uuid-123"
   
7. Результат:
   ✅ В базе: новая запись в таблице templates
   ✅ В проекте: связь через templateFilename
   ✅ В UI: кнопка "Выбрать шаблон" → "Файл: Customer Support"
```

### Сценарий 2: Использование существующего шаблона

```
1. Пользователь нажимает "Выбрать шаблон"
   └─> Открывается выпадающее меню
   
2. Frontend загружает список: GET /api/templates
   └─> Возвращает: [{ id: "uuid-123", name: "Customer Support", ... }]
   
3. Пользователь наводит на "Customer Support"
   └─> Frontend: GET /api/templates/uuid-123
   └─> Показывает preview справа
   
4. Пользователь кликает на шаблон
   └─> handleSelectTemplate(template)
   
5. Frontend обновляет проект:
   └─> block.template = template.content  (КОПИЯ текста)
   └─> block.templateFilename = template.id
   
6. Результат:
   ✅ Текст скопирован в редактор
   ✅ Связь установлена через templateFilename
   ✅ Пользователь может редактировать текст
```

### Сценарий 3: Обновление привязанного шаблона

```
1. Пользователь редактирует текст промпта
   └─> block.template изменяется в реальном времени
   
2. Нажимает "Сохранить шаблон"
   └─> Проверка: есть ли block.templateFilename?
   └─> Да → doUpdateTemplate(templateId)
   
3. Frontend → Backend: PATCH /api/templates/uuid-123
   Body: { content: "Updated dear customer..." }
   
4. Backend → Database: UPDATE templates SET content = ...
   
5. Результат:
   ✅ Шаблон в базе обновлен
   ✅ Другие проекты увидят старую версию (пока не выберут заново)
   ⚠️  Текущий проект продолжает использовать свою копию
```

### Сценарий 4: "Сохранить как" (дублирование)

```
1. Пользователь редактирует привязанный шаблон
   └─> block.template изменен
   └─> block.templateFilename = "uuid-123"
   
2. Нажимает "Сохранить как"
   └─> handleSaveTemplateFileAs()
   
3. Вводит новое имя: "Customer Support v2"
   └─> doCreateTemplate("Customer Support v2")
   
4. Frontend → Backend: POST /api/templates
   Body: { name: "Customer Support v2", content: "..." }
   
5. Backend → Database: INSERT (новая запись)
   └─> Возвращает: { id: "uuid-456", ... }
   
6. Frontend обновляет проект:
   └─> block.templateFilename = "uuid-456"  (НОВЫЙ ID)
   
7. Результат:
   ✅ В базе: 2 шаблона (uuid-123 и uuid-456)
   ✅ В проекте: связь с новым uuid-456
   ✅ Старый шаблон uuid-123 не изменен
```

---

## ⚙️ Особенности реализации

### ✅ Преимущества текущей архитектуры

1. **Изоляция данных:**
   - Каждый пользователь видит только свои шаблоны
   - Cascade delete при удалении пользователя

2. **Гибкость редактирования:**
   - Копирование шаблона в проект → можно редактировать
   - Исходный шаблон остается нетронутым

3. **Опциональная связь:**
   - Можно работать без привязки к шаблонам
   - Можно создать промпт "с нуля" без шаблона

4. **Кеширование (React Query):**
   - Список шаблонов кешируется
   - Автоматическая инвалидация при изменениях
   - Меньше запросов к серверу

5. **Поиск:**
   - Серверный поиск по `name` и `content`
   - Клиентский фильтр в UI для быстрого отклика

### ⚠️ Недостатки и потенциальные проблемы

1. **Двойное хранение данных:**
   - Текст шаблона дублируется в проекте
   - Увеличивает размер JSONB поля `project.data`
   - Если шаблон обновляется, старые проекты используют старую версию

2. **Нет версионирования:**
   - История изменений не сохраняется
   - Невозможно откатиться к предыдущей версии
   - Невозможно сравнить версии

3. **Нет синхронизации:**
   - Обновление шаблона не влияет на существующие проекты
   - Нужно вручную "выбрать шаблон" заново

4. **Отсутствие категорий/тегов:**
   - Все шаблоны в одном списке
   - Нет папок или группировки
   - Поиск только по тексту

5. **Нет шаринга шаблонов:**
   - Невозможно поделиться шаблоном с другим пользователем
   - Нет публичных шаблонов

6. **Лимиты не реализованы:**
   - Нет ограничения на количество шаблонов
   - Нет проверки размера `content`

---

## 🚀 Рекомендации по улучшению

### 1. Версионирование шаблонов

**Предложение:**
```prisma
model TemplateVersion {
  id String @id @default(uuid())
  
  templateId String @map("template_id")
  template   Template @relation(fields: [templateId], references: [id], onDelete: Cascade)
  
  version Int
  content String @db.Text
  
  createdAt DateTime @default(now())
  createdBy String @map("created_by") // userId
  
  @@index([templateId])
  @@map("template_versions")
}
```

**Преимущества:**
- История всех изменений
- Возможность отката
- Сравнение версий

### 2. Категории и теги

**Предложение:**
```prisma
model TemplateCategory {
  id String @id @default(uuid())
  userId String
  name String
  
  templates Template[]
  
  @@map("template_categories")
}

model TemplateTag {
  id String @id @default(uuid())
  name String
  
  templates Template[]
  
  @@map("template_tags")
}
```

**Преимущества:**
- Организация больших библиотек
- Быстрый поиск по категориям
- Улучшенная навигация

### 3. Публичные шаблоны

**Предложение:**
```prisma
model Template {
  // ... existing fields
  
  isPublic Boolean @default(false)
  visibility String @default("private") // private | team | public
  
  usageCount Int @default(0)
  rating Float?
  
  @@map("templates")
}
```

**Преимущества:**
- Marketplace шаблонов
- Шаринг между пользователями
- Рейтинги и популярность

### 4. Синхронизация шаблонов

**Предложение:**
Добавить флаг `syncWithTemplate`:

```typescript
interface PromptBlock {
  // ... existing fields
  syncWithTemplate?: boolean;  // Автообновление при изменении шаблона
}
```

**Логика:**
- Если `syncWithTemplate = true` → загружать свежую версию при открытии проекта
- Если `syncWithTemplate = false` → использовать копию

### 5. Импорт/Экспорт шаблонов

**Предложение:**
```typescript
// API endpoints
POST /api/templates/import
  Body: { file: FormData }  // JSON файл

GET /api/templates/:id/export
  Response: JSON файл для скачивания
```

**Преимущества:**
- Резервное копирование
- Перенос между аккаунтами
- Шаринг через файлы

### 6. Статистика использования

**Предложение:**
```prisma
model TemplateUsage {
  id String @id @default(uuid())
  
  templateId String
  userId String
  
  usedAt DateTime @default(now())
  projectId String?
  
  @@index([templateId])
  @@index([userId])
  @@map("template_usage")
}
```

**Преимущества:**
- Популярные шаблоны
- Аналитика использования
- Рекомендации

---

## 📝 Выводы

### Текущее состояние: ✅ РАБОТАЕТ СТАБИЛЬНО

**Что реализовано:**
- ✅ CRUD операции для шаблонов
- ✅ Связь шаблонов с проектами
- ✅ Поиск по имени и содержимому
- ✅ UI для выбора и редактирования
- ✅ Автосохранение в проектах
- ✅ Изоляция данных по пользователям

**Что можно улучшить:**
- 🔄 Версионирование
- 📁 Категории/теги
- 🌐 Публичные шаблоны
- 🔗 Синхронизация
- 📊 Статистика
- 💾 Импорт/Экспорт

### Архитектурные решения: ⚠️ КОМПРОМИССЫ

**Плюсы двойного хранения (template + templateFilename):**
- ✅ Гибкость редактирования
- ✅ Изоляция изменений
- ✅ Простая реализация

**Минусы:**
- ⚠️  Дублирование данных
- ⚠️  Нет автообновления
- ⚠️  Увеличенный размер проектов

**Рекомендация:** Добавить флаг `syncWithTemplate` для опционального автообновления.

---

## 🎯 Итоговая оценка

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| **Функциональность** | ⭐⭐⭐⭐☆ | Основные функции реализованы, но нет версий и категорий |
| **Производительность** | ⭐⭐⭐⭐⭐ | React Query кеширование, индексы в БД |
| **Масштабируемость** | ⭐⭐⭐☆☆ | Ограничения появятся при >1000 шаблонов |
| **UX** | ⭐⭐⭐⭐☆ | Удобный UI, но можно добавить drag-and-drop |
| **Безопасность** | ⭐⭐⭐⭐⭐ | Изоляция данных, авторизация на всех endpoints |
| **Поддерживаемость** | ⭐⭐⭐⭐☆ | Чистый код, но нет документации API |

**Общая оценка: 4.2/5.0 ⭐⭐⭐⭐☆**

**Система шаблонов работает надежно и покрывает базовые потребности. Для enterprise-уровня рекомендуется добавить версионирование, категории и публичные шаблоны.**

