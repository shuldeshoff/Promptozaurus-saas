# Changelog - 28 ноября 2025

## 🎯 Основная задача
Возврат левой панели управления проектами с функцией сворачивания по запросу заказчика.

---

## ✨ Реализованные функции

### 1. **Левая панель проектов (ProjectList)**
- Возвращена в интерфейс как основной способ управления проектами
- Заменила модальное окно ProjectManagerModal
- Расположена слева от основной рабочей области
- Ширина: 256px (w-64) в развернутом состоянии, 48px (w-12) в свернутом

### 2. **Сворачивание/разворачивание панели**
- Кнопка со стрелкой в заголовке панели для сворачивания
- Кнопка со стрелкой на узкой панели для разворачивания
- Состояние сохраняется в `localStorage` (`projectsPanelCollapsed`)
- Плавная анимация переходов
- UX аналогичен ChatGPT/Claude

### 3. **Переименование проектов**
- Клик по названию проекта активирует режим редактирования
- Inline input с автофокусом
- Сохранение: Enter или потеря фокуса (blur)
- Отмена: Escape
- Валидация: не сохраняет пустые названия
- Синхронизация с EditorContext для обновления активного проекта

### 4. **Расшаривание проектов**
- Кнопка "Share" (иконка) на каждой карточке проекта
- Inline форма расшаривания раскрывается внутри карточки
- Поле ввода email с валидацией
- Кнопка "Добавить" для создания share
- Список пользователей с доступом
- Кнопка удаления доступа для каждого пользователя
- Кнопка закрытия формы (X)
- Карточка не меняет цвет при открытой форме расшаривания
- Используются хуки: `useProjectShares`, `useCreateProjectShare`, `useDeleteProjectShare`

### 5. **Информация о проектах**
- **Дата обновления**: отображается в формате `toLocaleDateString()`
- **Счетчики блоков**: 
  - `X ctx` - количество блоков контекста
  - `X prm` - количество блоков промптов
- Компактное отображение в одну строку с разделителями (•)

### 6. **Действия с проектами**
- **Дублирование**: кнопка с иконкой копирования
- **Удаление**: кнопка с иконкой корзины, с подтверждением через ConfirmationModal
- Все кнопки с hover эффектами
- Disabled состояния во время выполнения операций

### 7. **Header упрощен**
- Удалена кнопка "Projects" (функционал перенесен в левую панель)
- Удален импорт и использование ProjectManagerModal
- Добавлен логотип "PromptyFlow" слева
- Сохранены кнопки: язык, справка, AI настройки, email, выход

---

## 🗑️ Удаленные компоненты

### ProjectManagerModal.tsx
- Полностью удален
- Весь функционал перенесен в левую панель ProjectList
- Это включало:
  - Создание проектов (осталось в ProjectList)
  - Переименование проектов (перенесено в ProjectList)
  - Удаление проектов (перенесено в ProjectList)
  - Расшаривание проектов (перенесено в ProjectList)

---

## 🎨 UI/UX изменения

### Дизайн карточек проектов
```
┌─────────────────────────────────────┐
│ Project Name (click to rename)  🔗📋🗑│
│ 28.11.2025 • 3 ctx • 5 prm         │
└─────────────────────────────────────┘
```

### Свернутая панель
```
┌──┐
│ ▶│ <- кнопка разворачивания
└──┘
```

### Развернутая панель
```
┌─────────────────────────┐
│ Projects    10/10    ◀ │ <- заголовок с кнопкой сворачивания
│ ┌─────────────────────┐ │
│ │ + Новый проект      │ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ My Project    🔗📋🗑│ │
│ │ 28.11.2025 • 3•5    │ │
│ └─────────────────────┘ │
│                         │
│ ┌─────────────────────┐ │
│ │ Another Project     │ │
│ │ 27.11.2025 • 1•2    │ │
│ │ ┌─────────────────┐ │ │
│ │ │ Share form      │ │ │ <- inline форма
│ │ │ [email] [Add]   │ │ │
│ │ │ user@email ❌   │ │ │
│ │ └─────────────────┘ │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

---

## 🔧 Технические детали

### Новые состояния в ProjectList
```typescript
const [editingProjectId, setEditingProjectId] = useState<string | null>(null);
const [editingProjectName, setEditingProjectName] = useState('');
const [sharingProjectId, setSharingProjectId] = useState<string | null>(null);
const [newShareEmail, setNewShareEmail] = useState('');
```

### Интеграция с DashboardPage
```typescript
const [isProjectsPanelCollapsed, setIsProjectsPanelCollapsed] = useState(() => {
  const saved = localStorage.getItem('projectsPanelCollapsed');
  return saved ? JSON.parse(saved) : false;
});

useEffect(() => {
  localStorage.setItem('projectsPanelCollapsed', JSON.stringify(isProjectsPanelCollapsed));
}, [isProjectsPanelCollapsed]);
```

### Компонент ProjectCard
- Выделен в отдельную функцию внутри ProjectList.tsx
- Принимает все необходимые props и callbacks
- Использует `useProjectShares` для загрузки списка shares
- Условный рендеринг формы расшаривания

---

## 🌍 Локализация

### Новые ключи (RU/EN)
```json
{
  "labels": {
    "showProjects": "Показать проекты / Show Projects",
    "hideProjects": "Скрыть проекты / Hide Projects",
    "clickToRename": "Нажмите для переименования / Click to rename"
  },
  "buttons": {
    "adding": "Добавление... / Adding..."
  }
}
```

---

## 📊 Статистика изменений

- **Файлов изменено**: 6
- **Строк добавлено**: ~476
- **Строк удалено**: ~552 (включая удаление ProjectManagerModal)
- **Новых компонентов**: 1 (ProjectCard внутри ProjectList)
- **Удаленных компонентов**: 1 (ProjectManagerModal)
- **Новых хуков использовано**: 3 (useProjectShares, useCreateProjectShare, useDeleteProjectShare)

---

## ✅ Выполненные TODO

1. ✅ Вернуть ProjectList как левую панель с collapse функцией
2. ✅ Убрать кнопку Projects из Header
3. ✅ Добавить кнопку toggle для сворачивания панели
4. ✅ Сохранять состояние панели в localStorage
5. ✅ Удалить ProjectManagerModal
6. ✅ Добавить переименование проектов по клику
7. ✅ Добавить функцию расшаривания в карточки проектов
8. ✅ Добавить отображение статуса и даты обновления
9. ✅ Добавить счетчики блоков контекста и промптов

---

## 🎯 Соответствие требованиям заказчика

### ✅ Выполнено
- Левая панель проектов возвращена
- Возможность сворачивать/разворачивать панель (как в ChatGPT/Claude)
- Создание проектов
- Переименование проектов
- Удаление проектов
- Расшаривание проектов
- Все без эмодзи (используются SVG иконки)

### 📝 Для будущей реализации
- Лимит на размер проекта (500KB - 1MB)
- Оптимистическая блокировка для одновременного редактирования
- Отображение статуса "Расшарен" для проектов, которыми поделились с пользователем
- Библиотека промптов (marketplace)
- Экспорт в DOCX/PDF

---

## 🚀 Деплой

Изменения закоммичены и запушены в репозиторий:
```
commit ddac204
feat: Возврат левой панели проектов с расширенным функционалом
```

После деплоя пользователям может потребоваться жесткая перезагрузка страницы (Ctrl+Shift+R / Cmd+Shift+R) для обновления кэша.

