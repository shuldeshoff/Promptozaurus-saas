# 🔍 Отчёт: Устаревшие упоминания локальной файловой системы

**Дата:** 30 ноября 2025  
**Проект:** Promptozaurus SaaS

---

## 📋 Найденные упоминания файловой системы

### 1. ✅ **УЖЕ ИСПРАВЛЕНО**

#### `apps/web/src/locales/ru/editor.json`
- ❌ ~~"Нет файлов .txt"~~ → ✅ "Нет сохранённых шаблонов"
- ❌ ~~"Введите имя файла (без .txt)"~~ → ✅ "Введите название шаблона"
- ❌ ~~"Файл: Название"~~ → ✅ "Шаблон: Название"
- ❌ ~~"Указать директорию для txt–шаблонов"~~ → ✅ "Управление шаблонами в библиотеке"

#### `apps/web/src/locales/en/editor.json`
- ❌ ~~"No .txt files"~~ → ✅ "No saved templates"
- ❌ ~~"Enter file name (without .txt)"~~ → ✅ "Enter template name"
- ❌ ~~"File: Name"~~ → ✅ "Template: Name"
- ❌ ~~"Specify directory for txt templates"~~ → ✅ "Manage templates in library"

---

### 2. ⚠️ **ТРЕБУЕТ ИСПРАВЛЕНИЯ**

#### `apps/web/src/locales/ru/header.json`

**Проблемные строки:**

| Строка | Текущее значение | Контекст | Используется? |
|--------|-----------------|----------|---------------|
| `project.projectsFolder` | "Папка проектов" | Legacy кнопка выбора папки | ❌ НЕТ |
| `context.folder` | "Папка контекстов" | Legacy кнопка выбора папки | ❌ НЕТ |
| `tooltips.selectProjectsFolder` | "Выбрать папку для сохранения проектов" | Tooltip для несуществующей кнопки | ❌ НЕТ |
| `tooltips.importContextBlock` | "Импортировать блок контекста" | Функция не реализована | ❌ НЕТ |
| `tooltips.selectContextFolder` | "Выбрать папку для блоков контекста" | Tooltip для несуществующей кнопки | ❌ НЕТ |

**Откуда взялись:**
```javascript
// originals/src/components/layout/Header.js (Electron версия)
const handleChooseProjectsFolder = async () => {
  const folder = await window.electronAPI.selectDirectory();
  if (folder) {
    actions.setProjectFolder(folder);
  }
};

const handleChooseContextDataFolder = async () => {
  const folder = await window.electronAPI.selectDirectory();
  if (folder) {
    actions.setContextDataFolder(folder);
  }
};
```

**В SaaS версии:**
- ❌ Нет кнопок выбора папок в Header
- ❌ Нет `electronAPI.selectDirectory()`
- ❌ Нет локальных папок для проектов/контекста
- ✅ Всё хранится в PostgreSQL

#### `apps/web/src/locales/en/header.json`

**Проблемные строки:**

| Строка | Текущее значение | Контекст | Используется? |
|--------|-----------------|----------|---------------|
| `project.projectsFolder` | "Projects Folder" | Legacy кнопка выбора папки | ❌ НЕТ |
| `context.folder` | "Context Folder" | Legacy кнопка выбора папки | ❌ НЕТ |
| `tooltips.selectProjectsFolder` | "Select folder for saving projects" | Tooltip для несуществующей кнопки | ❌ НЕТ |
| `tooltips.importContextBlock` | "Import a context block" | Функция не реализована | ❌ НЕТ |
| `tooltips.selectContextFolder` | "Select folder for context blocks" | Tooltip для несуществующей кнопки | ❌ НЕТ |

---

### 3. ℹ️ **ВАЛИДНЫЕ УПОМИНАНИЯ (оставить без изменений)**

#### `apps/web/src/locales/ru/common.json`
```json
{
  "labels": {
    "folder": "Папка",     // ✅ Общий термин, может использоваться в других контекстах
    "file": "Файл"          // ✅ Общий термин, может использоваться в других контекстах
  },
  "actions": {
    "import": "Импорт",     // ✅ Импорт может быть через API или upload
    "export": "Экспорт"     // ✅ Экспорт работает (JSON download)
  }
}
```

#### `apps/web/src/locales/ru/blockItem.json`
```json
{
  "context": {
    "export": "Экспортировать блок"  // ✅ РЕАЛИЗОВАНО: скачивает JSON
  }
}
```

**Реализация экспорта:**
```typescript
// apps/web/src/components/context/ContextBlockItem.tsx (строки 82-100)
const handleExportBlock = async (e: React.MouseEvent) => {
  e.stopPropagation();
  
  // Формируем JSON для экспорта
  const exportData = {
    title: block.title,
    items: block.items,
  };

  const dataStr = JSON.stringify(exportData, null, 2);
  const dataUri = 'data:application/json;charset=utf-8,' + encodeURIComponent(dataStr);

  const exportFileDefaultName = `${block.title.replace(/[^a-z0-9]/gi, '_').toLowerCase()}.json`;

  const linkElement = document.createElement('a');
  linkElement.setAttribute('href', dataUri);
  linkElement.setAttribute('download', exportFileDefaultName);
  linkElement.click();
};
```
✅ **Экспорт работает через Data URI** - браузер скачивает JSON файл.

#### `apps/web/src/locales/ru/notifications.json`
```json
{
  "project": {
    "exported": "Проект экспортирован",   // ✅ Если будет реализован
    "imported": "Проект импортирован"      // ✅ Если будет реализован
  },
  "context": {
    "imported": "Контекст импортирован",   // ✅ Если будет реализован
    "importFailed": "Не удалось импортировать контекст"  // ✅ Если будет реализован
  }
}
```
⚠️ **Импорт контекста/проектов НЕ РЕАЛИЗОВАН** в текущей версии, но строки могут пригодиться для будущей реализации.

---

## 🔧 Рекомендации по исправлению

### Вариант 1: Удалить неиспользуемые ключи

**`apps/web/src/locales/ru/header.json`:**
```json
{
  "project": {
    "label": "Проект:",
    "new": "Создать",
    "open": "Открыть",
    "save": "Сохранить",
    "saveAs": "Сохранить как...",
    // ❌ УДАЛИТЬ: "projectsFolder": "Папка проектов",
    "currentPath": "Проект: {{path}}",
    "newProject": "Новый проект",
    "untitled": "Без названия"
  },
  "context": {
    // ❌ УДАЛИТЬ: "import": "Импорт контекста",
    // ❌ УДАЛИТЬ: "folder": "Папка контекстов"
  },
  "tooltips": {
    "createNewProject": "Создать новый проект",
    "openExistingProject": "Открыть существующий проект",
    "saveCurrentProject": "Сохранить текущий проект",
    "saveProjectWithNewName": "Сохранить проект с новым именем",
    // ❌ УДАЛИТЬ: "selectProjectsFolder": "Выбрать папку для сохранения проектов",
    "editProjectName": "Нажмите для редактирования названия проекта",
    // ❌ УДАЛИТЬ: "importContextBlock": "Импортировать блок контекста",
    // ❌ УДАЛИТЬ: "selectContextFolder": "Выбрать папку для блоков контекста",
    "aiSettings": "Настройки искусственного интеллекта",
    "sendToAI": "Отправить промпт в AI",
    "exitApp": "Выйти из приложения",
    "switchLanguage": "Переключить язык",
    "help": "Показать быструю справку (F1)",
    "shareProject": "Поделиться проектом с другими пользователями"
  }
}
```

### Вариант 2: Переименовать для будущего использования

**Если планируется реализация импорта:**
```json
{
  "context": {
    "import": "Импортировать контекст из JSON",  // Будущая функция
    "importTooltip": "Загрузить блок контекста из файла"
  }
}
```

---

## 📊 Сводная таблица

| Файл | Статус | Действие |
|------|--------|----------|
| `apps/web/src/locales/ru/editor.json` | ✅ Исправлено | Уже задеплоено |
| `apps/web/src/locales/en/editor.json` | ✅ Исправлено | Уже задеплоено |
| `apps/web/src/locales/ru/header.json` | ⚠️ Требует исправления | Удалить неиспользуемые ключи |
| `apps/web/src/locales/en/header.json` | ⚠️ Требует исправления | Удалить неиспользуемые ключи |
| `apps/web/src/locales/ru/common.json` | ✅ Валидно | Общие термины |
| `apps/web/src/locales/en/common.json` | ✅ Валидно | Общие термины |
| `apps/web/src/locales/ru/blockItem.json` | ✅ Валидно | Экспорт реализован |
| `apps/web/src/locales/en/blockItem.json` | ✅ Валидно | Экспорт реализован |
| `apps/web/src/locales/ru/notifications.json` | ℹ️ Для будущего | Импорт не реализован |
| `apps/web/src/locales/en/notifications.json` | ℹ️ Для будущего | Импорт не реализован |

---

## 🎯 Итоги

### Что работает:
- ✅ **Экспорт контекста** → скачивает JSON файл через Data URI
- ✅ **Шаблоны** → хранятся в PostgreSQL, нет упоминаний `.txt`

### Что не работает (legacy):
- ❌ **Кнопки выбора папок** в Header (Electron API)
- ❌ **Импорт контекста** из файлов (не реализовано)
- ❌ **Импорт проектов** из файлов (не реализовано)

### Рекомендация:
**Удалить неиспользуемые ключи** из `header.json` для устранения путаницы.

