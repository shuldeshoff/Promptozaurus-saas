# PromptyFlow v0.8 - Visual Context Selection

## Основные изменения

### 🎨 Новый визуальный интерфейс выбора контекстов

В версии 0.8 реализован принципиально новый способ выбора контекстных элементов для промптов.

#### До версии 0.8 (старый подход):
- Выбор элементов через чекбоксы
- Отсутствие визуального порядка выбора
- Сложность при работе с большим количеством элементов

#### После версии 0.8 (новый подход):
- **Визуальные тайлы** вместо чекбоксов
- **Drag-select** - выбор элементов курсором мыши
- **Номера порядка** на каждом выбранном элементе
- **Цветовая индикация** выбранных элементов
- **Быстрые действия** - кнопки выбора всех/снятия выбора

### Новые возможности

#### Drag-Select (Выбор курсором)
- Нажмите и удерживайте мышь на элементе
- Проведите курсором по другим элементам для их выбора/снятия выбора
- Первый элемент определяет режим: если был выбран - снимается выбор, если не выбран - выбирается

#### Визуальные тайлы
- **Основные элементы** - крупные тайлы справа
- **Подэлементы** - компактные тайлы слева
- **Номер порядка** в круге слева от названия
- **Размер контента** отображается на каждом тайле
- **Цветовая схема**:
  - Серый - доступен для выбора
  - Зелёный - выбран
  - Полупрозрачный - недоступен (пустой контент)

#### Быстрые действия
- **Выбрать все** - выбор всех элементов в секции
- **Снять все** - снятие выбора в секции
- **Выбрать подэлементы** (зелёная кнопка +) - выбор всех подэлементов элемента
- **Снять подэлементы** (красная кнопка ×) - снятие выбора подэлементов
- **Копировать** - копирование выбранного контекста в буфер обмена

#### Панель предпросмотра
- Количество выбранных элементов
- Общее количество символов
- Кнопка копирования выбранного контекста

### Преимущества нового интерфейса

1. **Скорость работы** - drag-select намного быстрее чекбоксов
2. **Визуальная ясность** - сразу видно порядок и количество выбранных элементов
3. **Удобство** - меньше кликов для достижения результата
4. **Интуитивность** - естественный способ взаимодействия
5. **Mobile-friendly** - адаптирован для мобильных устройств

### Сохранение порядка выбора

Порядок, в котором вы выбираете элементы, теперь сохраняется:
- При компиляции промпта элементы подставляются в порядке выбора
- Порядок сохраняется при переключении между промптами
- Порядок экспортируется вместе с проектом

### Горячие клавиши

- `Ctrl/Cmd + A` - выбрать все элементы
- `Ctrl/Cmd + D` - снять все выборы
- `Ctrl/Cmd + C` - копировать выбранный контекст

## Технические детали

### Новые компоненты

```
apps/web/src/
├── components/
│   └── context-selection/
│       ├── ContextSelectionPanel.tsx  # Главная панель
│       ├── ContextSectionGrid.tsx     # Секция блока
│       ├── ContextTile.tsx            # Тайл элемента
│       └── index.ts
├── hooks/
│   └── useDragSelect.ts               # Хук drag-select
└── components/
    └── context-selection.css          # Стили
```

### Обновлённые типы

```typescript
interface PromptBlock {
  // ... existing fields
  selectionOrder?: string[];  // Новое поле
}
```

Поле `selectionOrder` содержит массив ключей в формате:
- `"blockId-itemId"` для основных элементов
- `"blockId-itemId-subItemId"` для подэлементов

### API изменения

Никаких изменений в API не требуется - новое поле `selectionOrder` опционально и сохраняется в существующем поле `data` проекта.

## Миграция

Проекты, созданные в старых версиях, автоматически работают с новым интерфейсом:
- При первом открытии промпта `selectionOrder` генерируется из `selectedContexts`
- При последующих изменениях `selectionOrder` обновляется и сохраняется
- Обратная совместимость полностью сохранена

## Обратная связь

Если вы обнаружили проблемы или у вас есть предложения по улучшению нового интерфейса, создайте issue в репозитории проекта.

