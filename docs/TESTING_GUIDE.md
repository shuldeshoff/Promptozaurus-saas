# Инструкция по тестированию Promptozaurus

## Цель тестирования
Проверить соответствие функциональности приложения референсному проекту.

---

## 1. Работа с блоками контекста

### 1.1 Создание блоков контекста
- [x] Создать несколько тестовых блоков контекста
- [x] Убедиться, что блоки отображаются в левой панели
- [x] Проверить возможность выбора и активации блока

### 1.2 Работа с элементами и субблоками
- [x] **Создание элементов блока**: Нажать кнопку "+" на уровне блока контекста
- [x] **Создание субблоков**: Нажать кнопку "+" внутри элемента для создания субблоков
- [x] **Раскрытие/сворачивание**: Проверить кнопки раскрытия и сворачивания субблоков
- [x] Убедиться, что иерархия элементов отображается корректно

---

## 2. Разделение текста (Split функция)

### 2.1 Разделение текста на субблоки
**Тестовый сценарий:**
1. Создать элемент с большим текстом
2. Нажать кнопку "Ножницы" (Split)
3. Выбрать **"Разделить на субблоки"**
4. Указать количество частей (например, 5)
5. Выбрать метод деления: **"По концу предложения"**
6. Нажать **"Предпросмотр"** для проверки результата
7. Проверить, что текст разделен корректно
8. Опционально: Отметить **"Сохранить исходный текст"** или **"Удалить исходный"**
9. Нажать **"Применить"**

**Ожидаемый результат:**
- [x] Создано 5 субблоков под исходным элементом
- [x] Текст разделен примерно по указанным границам
- [x] Исходный текст сохранен или удален согласно настройкам

### 2.2 Разделение текста на основные элементы
**Тестовый сценарий:**
1. Взять тот же элемент с текстом
2. Нажать кнопку "Ножницы" (Split)
3. Выбрать **"Разделить на основные элементы"**
4. Указать количество частей (например, 3)
5. Отметить **"Удалить исходный текст"**
6. Нажать **"Предпросмотр"** (1, 2, 3 части)
7. Нажать **"Применить"**

**Ожидаемый результат:**
- [x] Исходный элемент разделен на 3 отдельных элемента того же уровня
- [x] Первая часть осталась в исходном элементе
- [x] Созданы 2 новых элемента со второй и третьей частями
- [x] Исходный большой текст удален

---

## 3. Настройка AI провайдеров

### 3.1 Добавление API ключа
**Тестовый сценарий (на примере OpenRouter):**
1. Открыть **"Настройки AI"**
2. Выбрать провайдера (например, **OpenRouter**)
3. Вставить API ключ
4. Нажать **"Проверить ключ"**

**Ожидаемый результат:**
- [x] Ключ успешно проверен
- [x] Статус изменился на **"Active"** (зеленый)

### 3.2 Настройка моделей
**Тестовый сценарий:**
1. После добавления ключа, открыть **"Настройки моделей"**
2. Нажать **"Обновить список доступных моделей"** (если провайдер поддерживает API списка моделей)
3. Выбрать модель из списка (например, **"gpt-4o-mini"** или **"meta-llama"**)
4. Настроить параметры:
   - Temperature (например, 0.7)
   - Max tokens (например, 2000)
5. Нажать **"Добавить конфигурацию"**
6. Опционально: Добавить вторую модель (например, **"gpt-3.5-turbo"**)
7. Установить одну из моделей **"По умолчанию"**

**Ожидаемый результат:**
- [x] Список моделей загружен (захардкожен для всех провайдеров)
- [x] Модели добавлены в конфигурацию
- [x] Одна модель установлена по умолчанию
- [x] Конфигурации сохранены на сервере (Redis)

**Примечание для провайдеров без API списка моделей:**
- [x] Для **всех провайдеров**: модели захардкожены в коде
- [x] Список доступных моделей отображается из предустановленного списка

### 3.3 Глобальные настройки AI
Проверить третий пункт меню:
- [x] **Timeout** (тайм-аут запроса) - реализовано
- [x] **Max retries** (количество попыток повтора) - реализовано
- [x] Другие глобальные параметры (streaming, auto-save) - реализовано

---

## 4. Работа с промптами и отправка запросов

### 4.1 Создание промпта
**Тестовый сценарий:**
1. Перейти на вкладку **"Промпты"**
2. Создать новый блок промпта
3. В правой панели выбрать элементы из **"Карты контекста"**
4. Например, выбрать **"Рассказ/История текста"**
5. Проверить, что выбранный контекст отображается в промпте

**Ожидаемый результат:**
- [x] Карта контекста отображается полностью (Context Selection Panel)
- [x] Можно выбрать элементы контекста (drag-select поддерживается)
- [x] Выбранный контекст добавляется в промпт (с сохранением порядка)

### 4.2 Настройка промпта
**Проверить:**
- [x] Текст промпта можно редактировать (через поле "Шаблон промпта")
- [x] Можно включить/отключить **"Оборачивание тегами"** (checkbox "Обернуть контекст в теги")
- [x] Пример: "Кратко перескажи роман" + контекст в тегах `<Context>...</Context>`
- [x] Плейсхолдер `{{context}}` для вставки выбранного контекста

### 4.3 Предпросмотр и копирование
- [x] Нажать **"Копировать полный промпт"** для копирования скомпилированного промпта
- [x] Просмотр скомпилированного промпта в секции "Готовый промпт"
- [x] Полноэкранный режим просмотра (кнопка редактора)
- [x] Убедиться, что текст скопирован корректно

### 4.4 Отправка через API
**Тестовый сценарий:**
1. Выбрать модель из списка (например, **"gpt-4o-mini"** или легкую модель **"nano"**)
2. Нажать **"Отправить в AI"** (иконка лампочки)
3. Дождаться ответа от модели

**Ожидаемый результат:**
- [x] Запрос отправлен успешно через AI Response Modal
- [x] Ответ получен и отображается
- [x] Быстрые модели (nano, flash) отвечают быстро
- [x] Качественные модели дают более детальные ответы

### 4.5 Сохранение ответа
**Тестовый сценарий 1 - Сохранение в новый блок:**
1. Получить ответ от AI
2. Нажать **"Сохранить ответ"**
3. В открывшейся панели опций ввести название нового блока (или оставить пустым для автоназвания)
4. Нажать **"Сохранить в новый блок"**

**Ожидаемый результат:**
- [x] Создан новый блок контекста с указанным названием
- [x] Ответ сохранен как элемент нового блока с заголовком "AI Response"
- [x] Блок отображается в левой панели
- [x] Автоматическое переключение на вкладку "Контекст"
- [x] Автоматическая активация созданного блока

**Тестовый сценарий 2 - Сохранение в существующий блок:**
1. Получить второй ответ от AI (например, от другой модели, например **"Gemini"**)
2. Нажать **"Сохранить ответ"**
3. В секции "Добавить в существующий блок" выбрать блок из выпадающего списка
4. Нажать **"Добавить в выбранный блок"**

**Ожидаемый результат:**
- [x] Ответ добавлен как новый элемент в выбранный блок
- [x] Элемент отображается в блоке
- [x] Можно открыть и просмотреть сохраненный ответ
- [x] Автоматическое переключение на вкладку контекста и активация блока

**Альтернатива:**
- [x] Кнопка **"Копировать"** (📋) - копирует ответ в буфер обмена без сохранения

---

## 5. Дополнительные функции для проверки

### 5.1 Экспорт блоков
**Тестовый сценарий:**
1. Открыть любой блок контекста с данными
2. Найти кнопку экспорта (иконка стрелки вверх) в заголовке блока
3. Нажать на кнопку экспорта

**Ожидаемый результат:**
- [x] Автоматически скачивается JSON файл с названием блока
- [x] Файл содержит:
  - Название блока (`title`)
  - Все элементы (`items`) с содержимым
  - Все подэлементы (`subItems`) с содержимым
- [x] Формат JSON корректный и читаемый (с отступами)
- [x] Файл можно использовать для импорта или обработки

### 5.2 Удаление элементов
**Тестовые сценарии:**

**Удаление элемента:**
1. Навести на элемент контекста
2. Нажать кнопку удаления (×) в правом верхнем углу тайла
3. Подтвердить удаление в модальном окне

**Ожидаемый результат:**
- [x] Появляется модальное окно подтверждения
- [x] После подтверждения элемент удаляется
- [x] Если элемент имел подэлементы, они тоже удаляются

**Удаление подэлемента:**
1. Навести на подэлемент
2. Нажать кнопку удаления (×)
3. Подтвердить удаление

**Ожидаемый результат:**
- [x] Удаляется только подэлемент
- [x] Родительский элемент остается

**Удаление целого блока:**
1. Нажать кнопку удаления (корзина) в заголовке блока
2. Подтвердить удаление

**Ожидаемый результат:**
- [x] Блок и все его элементы удаляются
- [x] Блок исчезает из левой панели

### 5.3 Перемещение элементов
**Тестовый сценарий:**

**Перемещение основных элементов:**
1. Навести на drag-handle (иконка с тремя линиями) слева от элемента
2. Зажать левую кнопку мыши
3. Перетащить элемент вверх или вниз
4. Отпустить кнопку мыши

**Ожидаемый результат:**
- [x] Элемент визуально перемещается (opacity 0.5 во время перетаскивания)
- [x] Порядок элементов изменяется
- [x] Изменения сохраняются на сервере

**Перемещение подэлементов:**
1. Навести на drag-handle подэлемента
2. Зажать и перетащить подэлемент
3. Отпустить кнопку мыши

**Ожидаемый результат:**
- [x] Подэлемент перемещается внутри своего родительского элемента
- [x] Порядок подэлементов изменяется
- [x] Изменения сохраняются на сервере

**Примечание:** Drag-and-drop реализован с помощью библиотеки @dnd-kit. Визуальная индикация перетаскивания включает полупрозрачность и drag-handle иконки.

### 5.4 Полноэкранный редактор
**Тестовый сценарий:**
1. Открыть элемент контекста для редактирования
2. Нажать сочетание **Ctrl+E** (или Cmd+E на Mac)
3. Редактировать текст в полноэкранном режиме
4. Нажать **"Сохранить"** или **Esc** для выхода

**Ожидаемый результат:**
- [x] Открывается полноэкранный редактор с текущим содержимым
- [x] Можно редактировать текст с подсветкой синтаксиса
- [x] Изменения сохраняются при нажатии "Сохранить"
- [x] Esc закрывает редактор без сохранения (с подтверждением если были изменения)

---

## 5.5 Детальное тестирование Context Selection Panel

**Описание:**
Context Selection Panel - визуальная панель выбора контекста с drag-select, аналогичная файловым менеджерам.

**Функции для проверки:**

**A. Drag-Select (выделение перетаскиванием):**
- [x] Зажмите ЛКМ на тайле и перетащите через несколько тайлов
- [x] Все тайлы под курсором выделяются
- [x] Режим определяется первым тайлом (добавление/удаление выбора)

**B. Визуальный порядок:**
- [x] На выбранных тайлах отображаются номера (1, 2, 3...)
- [x] Порядок сохраняется при компиляции промпта

**C. Кнопки быстрого выбора:**
- [x] "Выбрать все" / "Снять выбор" для секций
- [x] "Очистить выбор" внизу панели

**D. Статистика и копирование:**
- [x] Отображается количество элементов и символов
- [x] "Копировать выбранное" - компиляция и копирование в буфер

## 5.6 Детальное тестирование сохранения ответов AI

**Тестовый сценарий полного цикла:**
1. Получите ответ от AI
2. Нажмите **"Сохранить ответ"**

**Вариант А: Новый блок**
- [x] Введите название или оставьте пустым
- [x] Создается новый блок с элементом "AI Response"
- [x] Автопереключение на вкладку "Контекст"

**Вариант Б: Существующий блок**
- [x] Выберите блок из dropdown
- [x] Ответ добавляется как новый элемент
- [x] Блок автоматически активируется

---

## 6. Проверка UI и UX

### 6.1 Панели и навигация
- [x] Левая панель проектов сворачивается/разворачивается
- [x] Панель контекста отображает все блоки
- [x] Панель промптов отображает все промпты
- [x] Редактор отображает выбранный элемент
- [x] Context Selection Panel интегрирован в промпт-блоки
- [x] Drag-select работает плавно и интуитивно

### 6.2 Empty State (пустое состояние)
- [x] При отсутствии выбранного проекта отображается "Проект не выбран"
- [x] Кнопка "Открыть панель проектов" работает корректно
- [x] Кнопки создания блоков неактивны (серые) до выбора проекта

### 6.3 Локализация
- [x] Все тексты отображаются на выбранном языке (RU/EN)
- [x] Нет пропущенных ключей локализации
- [x] Модальные окна имеют корректные заголовки
- [x] Context Selection Panel полностью локализован
- [x] AI Config Modal полностью локализован

---

## Чек-лист основных функций

| Функция | Статус | Примечания |
|---------|--------|------------|
| Создание блоков контекста | ✅ | Полностью реализовано |
| Создание элементов и субблоков | ✅ | Полностью реализовано |
| Split на субблоки | ✅ | Полностью реализовано |
| Split на основные элементы | ✅ | Полностью реализовано |
| Добавление API ключей | ✅ | Реализовано для всех провайдеров |
| Проверка API ключей | ✅ | Реализовано тестирование ключей |
| Загрузка списка моделей | ✅ | Захардкожены в коде (OpenAI, Anthropic, Gemini, Grok, OpenRouter) |
| Настройка конфигураций моделей | ✅ | Полностью реализовано с сохранением на сервере |
| Создание промптов | ✅ | Полностью реализовано |
| Выбор контекста для промпта | ✅ | **Context Selection Panel с drag-select** |
| Предпросмотр промпта | ✅ | Полностью реализовано |
| Отправка запроса через API | ✅ | Реализовано с поддержкой всех провайдеров |
| Сохранение ответа в новый блок | ✅ | **Реализовано с выбором названия** |
| Сохранение ответа в существующий блок | ✅ | **Реализовано с выбором блока** |
| Экспорт блоков | ✅ | **Реализован экспорт в JSON** |
| Удаление элементов | ✅ | Реализовано для всех типов элементов |
| Полноэкранный редактор | ✅ | Полностью реализовано |

---

## Известные ограничения

### Google Gemini API
- **Проблема**: API ключ может не работать из-за региональных ограничений
- **Ошибка**: `"User location is not supported for the API use"`
- **Решение**: 
  1. Проверить настройки API ключа в Google Cloud Console
  2. Создать новый ключ без географических ограничений
  3. Убедиться, что IP сервера не заблокирован

### Провайдеры с хардкоженными моделями
- **Anthropic**: Список моделей захардкожен в коде (нет API для получения списка)
- **X.AI Grok**: Список моделей захардкожен

---

## Примечания для тестирования

1. **OpenRouter** - универсальный агрегатор, удобен для тестирования разных моделей
2. **Быстрые модели** (nano, flash) - используйте для быстрой проверки функциональности
3. **Качественные модели** (GPT-4, Claude) - для проверки качества ответов
4. **Split функция** - ключевая особенность проекта, тестировать тщательно
5. **Сохранение ответов** - проверить оба варианта (новый блок и существующий)

---

## Итоговый чек-лист
- [x] Все основные функции работают (**ВСЕ ключевые функции реализованы!**)
- [x] UI/UX соответствует ожиданиям
- [x] Нет критических ошибок
- [x] Локализация полная (EN/RU)
- [x] API интеграции работают (OpenAI, Anthropic, Gemini, Grok, OpenRouter)
- [x] Данные сохраняются корректно (PostgreSQL + Redis)

## Недавно реализованные функции (последние обновления)

### ✅ Drag-and-Drop перемещение элементов
**Дата: 30 ноября 2025 (поздний вечер)**
- Полноценное drag-and-drop для основных элементов контекста
- Drag-and-drop для подэлементов внутри родительского элемента
- Визуальные drag-handles (иконка с тремя линиями)
- Визуальная индикация при перетаскивании (opacity 0.5)
- Автоматическое сохранение нового порядка на сервере
- Использование библиотеки @dnd-kit для плавной работы

### ✅ Редактирование промптов с шаблонами
**Дата: 30 ноября 2025 (поздний вечер)**
- Поле "Шаблон промпта" для ввода текста промпта
- Поддержка плейсхолдера `{{context}}` для вставки контекста
- Checkbox "Обернуть контекст в теги" (`<Context>...</Context>`)
- Загрузка шаблонов из библиотеки
- Сохранение шаблонов в библиотеку
- Полноэкранный редактор для шаблона (Ctrl+E)

### ✅ Предпросмотр и копирование промптов
**Дата: 30 ноября 2025 (поздний вечер)**
- Секция "Готовый промпт" с компиляцией контекста
- Кнопка "Копировать полный промпт" для быстрого копирования
- Кнопка "Копировать шаблон" для копирования только шаблона
- Статистика символов (шаблон / контекст / итого)
- Полноэкранный просмотр скомпилированного промпта

### ✅ Сохранение ответов AI в контекст
**Дата: 30 ноября 2025 (вечер)**
- Сохранение ответа в новый блок контекста с кастомным названием
- Сохранение ответа в существующий блок (выбор из списка)
- Автоматическое переключение на вкладку контекста после сохранения
- Автоматическая активация блока с сохраненным ответом
- UI с опциями сохранения прямо в AI Response Modal

### ✅ Context Selection Panel с Drag-Select
**Дата: 30 ноября 2025**
- Реализован полноценный Context Selection Panel для промпт-блоков
- Drag-to-select как в файловых менеджерах
- Визуальное отображение порядка выбора (номера на тайлах)
- Поддержка items и sub-items
- Кнопки быстрого выбора/снятия выбора
- Копирование скомпилированного контекста
- Подсчет символов в реальном времени
- Полная локализация (EN/RU)

### ✅ AI Config с серверным хранением
**Дата: 30 ноября 2025**
- Вкладка "Провайдеры" - управление API ключами
- Вкладка "Модели" - настройка конфигураций моделей
- Вкладка "Настройки" - глобальные параметры (timeout, retry, streaming)
- Хранение в Redis через backend API
- Автоматический выбор модели по умолчанию в AI Assistant
- Интеграция с AIResponseModal

### ✅ Автоматическая активация проектов
**Дата: 28-29 ноября 2025**
- Новый проект автоматически становится активным после создания
- Empty State при отсутствии выбранного проекта
- Disabled состояние для кнопок без активного проекта

